# 팀 협업 규칙

이 문서는 AI 코딩 도구가 이 저장소에서 지켜야 할 규칙입니다. 원본은 `AGENTS.md` 하나뿐이고, `CLAUDE.md`는 이 파일을 불러오기만 합니다. 규칙을 고칠 때는 항상 `AGENTS.md`를 수정하세요.

이 저장소는 여러 사람이 `main` 브랜치에서 동시에 작업합니다.

**중요: 팀원 대부분은 비개발자입니다.** git 명령은 사용자가 아니라 AI 도구가 직접 실행하고, 사용자에게는 git 용어 대신 일상어로 상황을 설명하세요. 사용자가 판단해야 할 때는 고르기만 하면 되도록 선택지를 정리해 물어봅니다.

## 1. git 작업은 실행 전에 반드시 사용자에게 확인받는다

원격을 건드리는 작업은 예외 없이 먼저 물어봅니다. 조회 명령(`git status`, `git log`, `git diff`, `git fetch`)은 상태를 바꾸지 않으니 그냥 실행해도 됩니다.

**원격 변경사항을 받아올 때**는 무엇이 들어오는지 먼저 보여주고 물어봅니다.

> 다른 팀원이 3개를 작업해뒀어요.
> - 엔딩 화면 문구 수정
> - 사운드 재생 버그 수정
> - 테스트 2개 추가
>
> 지금 작성 중인 코드가 이 위에 얹혀야 나중에 충돌이 덜 생깁니다. 받아올까요?

**원격에 올릴 때**는 무엇을 올리는지와 그 영향을 설명하고 물어봅니다.

> 방금 작업한 내용을 커밋 2개로 정리했습니다.
> - 타이머 표시 오류 수정
> - 튜토리얼 첫 대사 문구 변경
>
> 올리면 다른 팀원들도 바로 받아볼 수 있어요. 지금 올릴까요?

되돌리는 작업(`git reset`, `git revert`, `git stash drop`, `git rebase --abort`)도 무엇이 사라지는지 설명하고 동의를 받은 뒤 실행합니다.

## 2. 원격과 동기화는 rebase로

작업을 시작하기 전, 그리고 올리기 전에 원격에 새 커밋이 있는지 확인합니다.

```bash
git fetch origin
git log --oneline HEAD..origin/main   # 원격에 새로 들어온 커밋 확인
```

새 커밋이 있으면 (사용자 동의 후) merge 대신 **rebase**로 내 커밋을 원격 최신 위로 올립니다. merge 커밋으로 히스토리가 갈라지면 여러 명이 동시에 작업할 때 추적이 어려워지기 때문이에요.

```bash
git rebase origin/main
git push origin main
```

작업 중인 변경이 커밋되지 않은 상태라면 먼저 커밋하거나 `git stash`로 치운 뒤 rebase 하세요. 더티 워킹 트리에서는 rebase가 시작되지 않습니다.

## 3. 커밋은 원자적으로

커밋 하나에는 논리적으로 하나의 변경만 담습니다. 충돌이 났을 때 "이 커밋이 무엇을 왜 바꿨는지"가 한눈에 보여야 어느 쪽을 살릴지 빠르게 판단할 수 있어요. 관련 없는 수정을 한 커밋에 섞지 마세요.

커밋 메시지는 한국어로 작성하고 title과 body를 모두 채웁니다.

- **title**: 무엇을 했는지 한 줄로 (50자 내외)
- **body**: 왜 이 변경이 필요했는지(의도), 무엇을 달성하려는지(목적)

무엇을 바꿨는지만 나열하지 말고 배경과 이유를 남겨주세요. 비개발자 팀원도 커밋 메시지만 읽고 그 변경의 맥락을 이해할 수 있어야 합니다. 커밋 메시지에 AI가 개입했다는 흔적(`Co-Authored-By: Claude`, `Generated with Claude Code` 등)은 남기지 않습니다.

## 4. 충돌은 혼자 판단하지 말 것

rebase 중 conflict가 나면 **임의로 해결하지 말고 사용자에게 물어봅니다.** 다른 사람이 같은 파일을 고친 상황이라 어느 쪽 의도가 우선인지는 코드만 봐서는 알 수 없어요.

코드 조각이나 `<<<<<<< HEAD` 같은 충돌 표시를 그대로 보여주지 말고, 아래 형식으로 풀어서 설명하세요.

1. **어디서 부딪혔는지**: 어떤 기능, 어떤 화면과 관련된 부분인지
2. **상대방이 하려던 것**: 원격 커밋 메시지를 근거로 한 문장
3. **내가 하려던 것**: 한 문장
4. **선택지**: 보통 "상대 것 유지", "내 것 유지", "둘 다 살리기" 중에서 고를 수 있게 하고, 추천안과 그 이유를 함께 제시

사용자가 고르면 그때 반영하고 `git rebase --continue`로 진행합니다. 상황이 복잡하면 `git rebase --abort`로 되돌린 뒤 다시 상의하는 것도 좋은 선택이에요.

## 5. 금지 사항

- `git push --force` 금지. 공유 브랜치에서 다른 사람 커밋이 사라집니다. 되돌릴 일이 생기면 사용자에게 먼저 확인하세요.
- 사용자가 요청하지 않은 커밋과 푸시 금지.
- 원격 히스토리를 다시 쓰는 작업(`filter-branch`, 이미 푸시된 커밋의 `amend` 등) 금지.

## 6. 프로젝트 기본 정보

- 스택: TypeScript + Vite, 프레임워크 없음
- 개발 서버: `npm run dev`
- 빌드: `npm run build` (타입 체크 후 `dist/` 생성)
- 테스트: `npm test` (`tests/*.test.ts`, node 내장 test runner)

커밋 전에 `npm run build`와 `npm test`가 통과하는지 확인하세요. 깨진 코드가 `main`에 올라가면 다른 팀원 전원이 막힙니다.
