# I.MY.ME.MINE — PRD (제품 방향 문서)

> **이 문서의 용도**: 팀 3인의 방향성 싱크업. "이 게임이 무엇이고, 왜 이렇게 만들며,
> 지금 어디까지 왔고, 다음에 무엇을 하는가"를 한 자리에서 답한다.
> 처음 합류했다면 이 문서 → [`README.md`](./README.md) 순서로 읽으면 된다.
> (마지막 갱신: 2026-08-21)

---

## 0. 한 장 요약

**I.MY.ME.MINE** 은 브라우저에서 링크만 열면 도는 60초 타임루프 탈출 퍼즐이다.
혼자서는 절대 못 여는 문 앞에서, 플레이어는 실패한 자신을 **잔상**(과거 플레이의 재생체)으로
남겨 동료로 쓴다. 몸은 넷뿐이고 — I, MY, ME, MINE — 그게 제목이자 시스템이다.

- **기술 정체성**: 잔상은 영상이 아니라 **입력 테이프**다. 매 루프 세계를 처음부터 다시
  시뮬레이션하므로 재생 오차가 정확히 0이고, 그 결정론 위에 15개 스테이지 전부
  "깰 수 있음 + 혼자서는 못 깸"이 테스트로 기계 증명돼 있다.
- **서사 정체성**: 시설은 능력을 누르는 곳이 아니라 **수확하는 곳**이다. 당신이 실수를
  지울 때마다(`DEBT`) 제품을 넘겨주고 있었다 — 게임 오버 대신 부채가 쌓이는 이유다.
- **지금 위치**: 코어 게임(15스테이지·4막·모드 2×2·마이크 LISTEN 모드·경비 4종·모바일
  터치)은 완성돼 배포 중. **막별 보스 4종이 구현 진행 중**(미커밋), **AI 킥 2종은
  방향만 확정되고 미착수**다.
- **다음 목표**: 보스 4종 마무리 → AI 킥(아이템 대장간 + 개인화 엔딩) → 발표 리허설.

---

## 1. 왜 이 게임인가 — 세 개의 기둥

모든 판단은 이 세 기둥으로 되돌아온다. 기능을 추가할지 말지 헷갈리면
"세 기둥 중 무엇을 강하게 하는가"를 물으면 된다.

### 기둥 1 — 실패가 자원이다

이 게임에는 게임 오버가 없다. 잡히면 그 루프의 나는 **시체가 되어 그 자리에서 발판을
누르고**, 실수를 통째로 지우려면 `DEBT`(런이 끝날 때까지 절대 리셋되지 않는 부채)를
치러야 한다. "실패 → 재시도"가 아니라 "실패 → 그 실패를 어디에 쓸까"가 이 게임의
기본 사고 단위다. 퍼즐 설계·서사·점수 체계가 전부 이 한 문장 위에 서 있다.

### 기둥 2 — 결정론이 곧 게임성이다

잔상이 매번 조금이라도 다르게 움직이면 이 게임은 성립하지 않는다. 그래서 시뮬레이션
내부에 부동소수점·난수·시계가 하나도 없고(전부 정수 연산), 그 대가로 이런 게임성이
나온다: 경비는 이번 루프의 **모든** 몸에 반응하고, 과거의 내가 경비를 끌고 가면 그
경비는 진짜로 거기 가 있다. 레이저는 외우는 게 아니라 **통과할 시각을 고르는** 대상이
된다. → 결정론을 깨는 기능 제안은 아무리 매력적이어도 기각한다(§5 하지 않을 것).

### 기둥 3 — 이야기는 메커니즘의 재해석이다

새 규칙을 더해 이야기를 만들지 않는다. **이미 있는 규칙의 의미만 뒤집는다.**
"게임 오버가 없다"는 관대함이 아니라 *그들은 당신을 죽일 생각이 없다(당신은 재고다)* 로,
"잔상이 남는다"는 편의 기능이 아니라 *회수 전까지 적재된 상태* 로 다시 읽힌다.
상세는 [`STORY.md`](./STORY.md)가 단일 출처다 — 대사·단서·엔딩을 쓸 땐 반드시 거기
정의된 사실만 쓴다.

---

## 2. 누구를 위해 만드는가

1순위 대상은 **해커톤 심사위원**이다. 구체적으로:

- **3분 안에 "아하"가 나와야 한다.** 스테이지 1은 par 1, Time-to-Aha 30초 목표로
  설계돼 있고, 심사용 데모 루트가 README에 분 단위로 잡혀 있다.
- **행사장 환경에서 죽지 않아야 한다.** 설치 0, 로그인 0, 런타임 네트워크 호출 0,
  마이크 캘리브레이션은 절대값이 아니라 환경음 대비 상대값(시끄러운 행사장 대응),
  마이크 실패 시 자동 EASY 폴백. 모바일 터치도 지원한다.
- **"정말 되는 거냐"는 질문에 코드로 답할 수 있어야 한다.** 주장마다 대응하는 테스트가
  있다(결정론·풀이 가능성·혼자서는 불가·무영향 보증). 심사 대응 논리는
  [`PITCH.md`](./PITCH.md)에 정리한다.

2순위는 심사 후에 링크를 받아 열어 보는 일반 플레이어다 — 그래서 스테이지 선택 없이
1번부터 순서대로, 인트로 만화로 맥락을 주고 속말 힌트로 막힘을 풀어 주는 구조다.

---

## 3. 스토리 — 처음 읽는 사람을 위한 요약

> 집필용 세부 규칙(호칭 사다리·금지 목록·막간 콘티)은 [`STORY.md`](./STORY.md)에 있다.
> 여기서는 팀원이 방향을 이해하는 데 필요한 뼈대만 적는다.

주인공은 이름 없는 시설에서 눈을 뜬다. 몸에는 **억제 코어**가 박혀 있고, 코어를 되찾아
봉인된 문을 열고 나가는 것이 표면의 목표다. 능력은 하나 — 잔상을 남기는 것.

반전은 3막에서 온다. **억제 코어는 능력을 누르는 장치가 아니라 모으는 장치다.**
시설은 감옥이 아니라 공장이고, 당신의 탈출 시도가 생산 공정이며, 당신이 버리고 간
잔상이 제품이다. 문 바깥쪽에 손잡이가 달려 있는 이유 — 나오라고 열어 둔 것이다.

이 공포의 축은 폭력이 아니라 **회계**다. 시설은 잔인하지 않고 사무적이다(점검표,
수율, 배치 기록). 엔딩은 누적 `DEBT`로 갈린다: 아무것도 두고 오지 않았다면(DEBT 0)
넷이 함께 나가고, 많이 버렸다면(DEBT 3+) 당신은 나가지만 시설에는 `수율 양호.` 한 줄이
찍히고 다음 배치가 깨어난다 — 가장 아프게 쓴 결말이 이쪽이다.

전달 방식의 원칙: 설명하지 않고 **알아채게 한다.** 막간 2컷 만화, `E`로 읽는 환경 단서
(강제 아님), 그리고 잔상을 부르는 호칭이 막마다 `저거` → `쟤` → `쟤도... 나였지` →
`우리` 로 옮겨 가는 속말 사다리. 4막의 `우리` 한 단어가 이 이야기의 도착점이다.

---

## 4. 현재 상태 — 무엇이 되어 있고 무엇이 남았나

정직하게 구분한다. "완료"는 main에 커밋되고 테스트가 지키는 것만 쓴다.

### 완료 (main 커밋 + 배포 + 테스트 보증)

- 코어 루프: 잔상 테이프 녹화/재생, 조기 확정(`R`), 덮어쓰기(`Q`, 스테이지당 1회),
  전체 초기화(`Backspace` = DEBT +1), 4바디 상한
- 15개 스테이지 · 4막, 막별 신규 장치 4종(grate·seqButton·laser·powerBus)
- 플레이 모드 축(STORY / GAUNTLET / TIME ATTACK) × 감지 축(EASY / LISTEN) 완전 직교
- LISTEN 모드: 실제 마이크 음량을 테이프에 녹화(잔상의 숨소리가 재현됨) —
  결정론 유지, 오디오 저장·전송 0, 실패 시 자동 폴백
- 경비 4종: SENTRY(기준) · HOUND(집요한 추적) · BRUTE(40px — 좁은 길에 못 들어옴) ·
  WATCHER(잡지 않고 경보를 울림), 상세 QA 및 회귀 테스트
- 눈뽕 아이템, 정위(방향성) 오디오, 모바일 터치 및 화면 복귀 처리
- 연출: 7칸 만화 인트로, 막간 만화, 상황 트리거 속말, 시설 레이어 렌더
- CI: 타입검사+전체 테스트 통과 없이는 배포 자체가 안 되는 파이프라인

### 진행 중 (작업 트리에 있음, 미커밋 — 완료라고 말하지 말 것)

- **막별 보스 4종**: 보스는 체력 많은 적이 아니라 **그 막에서 배운 규칙을 뒤집는 적**이다.
  - `INSPECTOR`(1막) — 두리번거리지 않고 시간표대로만 움직인다 → 시간표를 읽으면 혼자서도 지나간다
  - `PACK_LEAD`(2막) — 무리 전체와 추적 궤적을 공유한다 → 미끼 하나로는 안 되고, 미끼를 나눠야 한다
  - `OVERSEER`(3막) — 잔상 수에 비례해 강해진다 → par(최소 잔상)를 지키는 것이 처음으로 생존 조건이 된다
  - `COUNTER`(4막) — 잡지 않는다. **센다** → 들키지 않는 게 아니라 보이는 것 자체가 대가 (수확 전제와 직결)
  - `tests/boss.test.ts` 포함 전체 테스트(386개/98스위트)가 작업 트리 기준으로 통과한다
    (2026-08-21 실측). 즉 구현은 돌아가는 상태이나 **아직 main에 커밋되지 않았다** —
    레벨 배치·연출 확인 후 커밋이 남은 일이다. 대조군 원칙(대조군 없는 주장은 쓰지
    않는다)은 boss.test.ts 에도 유지돼 있다.
- STORY.md 보강(호칭 사다리 예외 규정, HUD 목표 줄 규칙) — 미커밋

### 방향 확정, 미착수

- **AI 킥 2종** ([`AI-KICK-IDEAS.md`](./AI-KICK-IDEAS.md)에서 10안 중 평가로 확정):
  - **메인 — AI 아이템 대장간**: 플레이어가 자연어로 아이템을 주문하면("경비를 3초 재우는
    다트") AI가 미리 정의된 효과 슬롯 × 수치 범위 안의 JSON으로 번역하고 클램프한다.
    심사위원이 무대에서 직접 주문해 볼 수 있는 유일한 안.
  - **서브 — 나만의 결말 생성기**: 플레이 로그(사망·잔상·소음)를 AI가 읽어 그 사람만의
    엔딩 서사와 칭호를 쓴다. "시설은 수확한다" 스토리와 정확히 맞물린다.
  - **불변 제약**: AI는 시뮬레이션 "이전"(파라미터 JSON까지)과 "이후/바깥"(서사)에만 선다.
    틱 판정 개입은 어떤 형태로든 금지 — 결정론 원칙 위반이므로.
  - 공통 기반 필요: GPT 호출 모듈 + **오프라인 캐시 폴백**(행사장 네트워크 사망 대비) +
    플레이 로그 수집기.

### 알려진 미검증 — 숨기지 않는다

- **사람 플레이테스트를 아직 하지 않았다.** 기계 증명은 "풀린다/혼자서는 안 풀린다"까지다.
  "재미있다"와 "데모 3분 배분이 실제로 맞다"는 검증되지 않은 주장이다. 팀이 3인이 된
  지금, 서로의 첫 플레이가 가장 싼 플레이테스트다 — 막힌 지점을 기록하자.

---

## 5. 하지 않을 것 (Non-goals)

제안 자체를 막는 게 아니라, **기각 사유를 재사용**하기 위해 적는다.

| 하지 않는 것 | 이유 |
|---|---|
| AI가 틱 단위 판정에 개입 (예: 발각 시 AI와 협상) | 결정론·잔상 재생·풀이 가능성 증명이 전부 무너진다. 기둥 2 위반 |
| AI 스테이지 즉석 생성 | "전 스테이지 클리어 가능 증명"을 AI 맵에는 적용할 수 없다. 못 깨는 맵이 데모에 나오면 역효과 |
| 외부 에셋(이미지·폰트·오디오 파일) 도입 | "에셋 0, 전부 코드 생성"이 강점의 일부다. 필요하면 절차 생성으로 |
| 런타임 네트워크 호출 (AI 킥 제외) | 행사장 안정성 + 개인정보 주장("오디오를 전송하지 않는다")의 근거 |
| 폭력·고문 묘사, 악당의 연설 | 이 이야기의 공포는 회계다. STORY.md §8 |
| 게임 오버 도입 | 기둥 1 위반. 실패는 벌이 아니라 자원이다 |

---

## 6. 팀 운영 — 문서 지도와 작업 규칙

### 문서 지도 (무엇이 어디의 단일 출처인가)

| 문서 | 단일 출처인 것 | 언제 읽나 |
|---|---|---|
| **PRD.md** (이 문서) | 방향·범위·우선순위·기각 사유 | 합류 첫날, 방향 논쟁이 생길 때 |
| [`README.md`](./README.md) | 게임 규칙 개요·데모 루트·실행/테스트 방법·아키텍처 요약 | 합류 첫날 두 번째로 |
| [`SPEC.md`](./SPEC.md) | 게임 규칙의 정밀 정의(수치·틱·판정) | 시뮬 코드를 만질 때 |
| [`STORY.md`](./STORY.md) | 서사 사실·대사 규칙·엔딩 | 대사·단서·연출을 쓸 때 |
| [`WALKTHROUGH.md`](./WALKTHROUGH.md) | 스테이지별 실측 공략 | 레벨을 고치거나 검증할 때 |
| [`AI-KICK-IDEAS.md`](./AI-KICK-IDEAS.md) | AI 킥 아이데이션과 평가 근거 | AI 킥 작업 전 |
| [`PRD-AI-STORY.md`](./0821%20seongwoo%20문서%20작업/PRD-AI-STORY.md) | AI 스토리 시스템(선택·변형·레거시) 기획 — 결정 대기 | AI 스토리 기능을 논의·구현할 때 |
| [`STORY-BRANCH.md`](./0821%20seongwoo%20문서%20작업/STORY-BRANCH.md) | 분기 서사·엔딩 4종·「새벽」 설계 — 결정 대기 | 분기 스토리를 논의·집필할 때 |
| [`PITCH.md`](./PITCH.md) | 해커톤 강점·심사 Q&A·발표 문장 | 발표 준비할 때 |

### 작업 규칙

1. **커밋 전 게이트**: `npx tsc --noEmit` + `npm test` 통과. CI가 어차피 막지만,
   로컬에서 먼저 돌리는 쪽이 빠르다. 시뮬(`src/sim/`)을 건드렸다면 결정론 테스트가
   지나가는지 특히 확인.
2. **주장에는 테스트를**: "X가 된다"를 README나 발표에 쓰려면 그걸 증명하는 테스트가
   있어야 한다. 기존 테스트 파일들이 전례다(대조군 포함 원칙).
3. **수치는 `constants.ts` 한 곳에만**: 매직 넘버 금지 규칙이 이미 코드베이스 전체에
   걸려 있다.
4. **서사 텍스트는 STORY.md 먼저**: 화면에 넣을 한국어(속말·단서·목표 줄)는 STORY.md의
   규칙(명령조 금지, 대사에 키 이름 금지, 호칭 사다리)을 먼저 확인하고 쓴다.
5. **문서도 코드처럼**: 방향이 바뀌면 이 PRD를 같은 커밋에서 고친다. 문서와 저장소가
   불일치하면 저장소가 정답이고, 문서를 저장소에 맞춘다.

---

## 7. 남은 기간 우선순위

순서에 이유가 있다: 보스가 코어 게임의 마지막 조각이고, AI 킥은 그 위에 얹는
차별화이며, 리허설 없는 데모는 도박이다.

1. **보스 4종 완성·커밋** — boss.test.ts 통과 + 기존 테스트 전체 무손상 확인
2. **AI 킥 공통 기반** — GPT 호출 모듈 + 캐시 폴백 + 로그 수집기 (로그 데이터는
   시뮬에 이미 다 있으므로 모으기만 하면 된다)
3. **AI 아이템 대장간** — 효과 슬롯 스키마 → 응답 검증·클램프 → 상점 UI →
   극단값 아이템으로 solvability 안 깨지는지 테스트 1개
4. **나만의 결말 생성기** — 같은 로그를 프롬프트만 바꿔 소비. 엔딩 선택지 1개로 분기 확보
5. **팀 내부 플레이테스트 + 데모 리허설** — 3분 배분 실측, 캐시 응답 적재(= 오프라인 대비)
