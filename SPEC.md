# I.MY.ME.MINE — 구현 명세 v1.0 (확정본)

> 실패할 때마다 과거의 내가 동료가 되는 60초 타임루프 하이스트.
> **Fail now. Steal later.**

이 문서는 `../afterimage_prd_draft_v0.1.md`(PRD v0.1)를 상위 문서로 하고,
구현 가능한 수준으로 결정을 못 박은 **개발 명세**다. PRD의 §33 Open Questions는 여기서 전부 닫는다.

---

## 0. 제출 컨텍스트 / 제약

- OpenAI Game Builders Seoul 2026 제출용.
- **게임 런타임에 OpenAI API 및 일체의 네트워크 호출을 사용하지 않는다.** 100% 로컬/정적.
  - AI 활용의 증거는 런타임이 아니라 **개발 프로세스**(자동 결정론 회귀 테스트 + 레벨 solvability 증명)로 제시한다. PRD §20–22와 일치.
- 스택: **TypeScript + Vite + Canvas 2D**. 런타임 의존성 0개 (Phaser/Pixi 미사용 — 결정론 통제를 위해 직접 루프).
- 심사위원이 `npm i && npm run dev` 로 즉시 플레이. 빌드 결과는 정적 파일(`base: './'`).
- 데스크톱 Chrome 우선, 60FPS.

## 0.1 PRD와의 차이 (의도적 결정)

| 항목 | PRD v0.1 | 본 명세 | 이유 |
|---|---|---|---|
| 제목 | AFTERIMAGE | **I.MY.ME.MINE** | AFTERIMAGE는 기출시작과 충돌. 네 격이 곧 몸 슬롯이 되어 제목=시스템. |
| 최대 잔상 | 5 | **3** (총 4바디) | PRD §11 예시 플레이가 정확히 4루프. 5는 화면 혼잡(Risk 3)만 키움. |
| 스택 | Phaser/Pixi 후보 | Canvas 2D 직접 | 결정론(§4)을 프레임워크에 위임할 수 없음. |
| 루프 종료 | 60초 or 실패 | **+ 언제든 조기 확정** | PRD가 안 다룬 대기시간 문제 해결. §3-1. |
| 리셋 | Open Question | **덮어쓰기 1회 + 유료 초기화** | PRD §33 "Manual Reset 허용 여부"의 답. §3-2. |
| 레벨 수 | 1 heist | **5** (교육 3 + 하이스트 2) | 3분 데모 루트(PRD §31)에 난이도 곡선이 필요. |

---

# 1. 코어 루프

플레이어는 항상 **한 명**을 조작한다. 조작 중 모든 입력은 고정 틱으로 녹화된다.
녹화가 확정되면(조기 확정 / 사망 / 시간초과) 그 런은 **잔상(afterimage)** 이 되고,
**스테이지 전체가 틱 0으로 리셋된 뒤** 모든 잔상이 동시에 재생되는 위에서 새 몸을 조작한다.

```
LOOP N 시작 → 월드를 틱 0으로 리셋
           → 잔상 1..N-1을 각자의 테이프로 재생 (동시)
           → 플레이어는 새 몸 조작 (동시)
           → 확정 → LOOP N+1
```

> **중요 아키텍처**: 잔상은 "저장된 궤적"이 아니라 **저장된 입력 테이프**다.
> 매 루프 월드를 처음부터 다시 시뮬레이션하며, 잔상에는 녹화된 입력 비트마스크를 먹인다.
> 따라서 경비병은 이번 루프에 존재하는 **모든** 몸(잔상 포함)에 반응하고,
> 그 반응까지 완전히 결정론적이다. 이게 "과거를 준비한다"는 PRD §36 원칙의 기계적 구현이다.

## 1.1 네 개의 몸 = 제목

| 슬롯 | 의미 | 상태 |
|---|---|---|
| **I** | 지금 조작 중인 나 | 항상 1명, 불투명 |
| **MY** | 첫 번째 확정된 나 | 잔상 |
| **ME** | 두 번째 | 잔상 |
| **MINE** | 세 번째 | 잔상 |

- 총 4바디 상한. 모든 스테이지는 4바디 이내로 클리어 가능해야 한다(§10에서 프로그램으로 증명).
- 잔상 3개가 찬 상태(= 4번째 몸)에서 또 확정되면 → **LOOP FAILED**, 전체 초기화 강제 + `DEBT +1`.
  - 이 순간의 카피: `NO ONE LEFT TO BECOME.`

---

# 2. 승리 / 패배

- **승리**: `loot` 획득 **AND** 조작 중인 몸(I)이 `escape` 존 도달. 잔상의 탈출은 무효(PRD §17).
- **루프 종료(= 잔상 확정)** 조건 3가지:
  1. `R` 조기 확정 (플레이어 의지)
  2. 경비에게 **체포**(CAPTURE) — 잔상은 그 자리에 **시체**로 정지
  3. 3600틱(60초) 소진 — 마지막 위치에 정지
- 게임 오버는 없다. 4바디를 다 쓰면 초기화될 뿐이다(DEBT 대가).

---

# 3. 세 가지 개선 (반드시 구현 — 이게 차별점이다)

### 3-1. 조기 확정 — 대기시간 제거
- 60초는 **최대치일 뿐**. `R` 로 언제든 즉시 확정.
- 잔상은 자기 테이프 길이만큼 재생하고 **그 이후 마지막 틱 상태로 정지한다**(사라지지 않음).
  - → 5초만 녹화해 발판 위에 서서 확정하면, 그 잔상은 남은 55초 내내 발판을 눌러준다.
  - → 조기 확정이 **전략**이 된다. 이게 없으면 플레이어는 매번 60초를 기다린다.
- HUD에 `R ▸ COMMIT` 상시 노출. 확정→다음 루프 전환 **2초 이내**(PRD §15).

### 3-2. 실패 누적 / 덮어쓰기 1회 / 유료 초기화
- 실패(체포·시간초과)한 런도 **자동으로 잔상이 된다. 버릴 수 없다.**
  - 체포된 잔상 = **시체**. 여전히 발판을 누르고, 여전히 경비의 시야를 끈다(미끼로 유효).
  - "실패한 나는 동료이자 지울 수 없는 부채."
- `Q` → 덮어쓰기 모드 → `1/2/3` 으로 잔상 슬롯 선택 → 그 슬롯을 지우고 그 자리에서 재녹화.
  **스테이지당 정확히 1회.** 소진 시 HUD 배지가 `○`로 바뀐다.
- `BACKSPACE` **2초 홀드** → 스테이지 전체 초기화. 언제든 가능. **`DEBT +1`.**
  - `DEBT`는 스테이지를 넘어가도 **절대 리셋되지 않는다**(세션 누적, 엔딩 화면에 표시).
  - → 실수를 공짜로 지울 수는 없다.

### 3-3. 하나의 잔상이 여러 역할 — "발판 숫자 맞히기" 탈출
PRD Risk 1(`Button → Door → Button → Door` 정답 퍼즐화)에 대한 구체적 대응.
장치는 잔상 하나가 **시간축 위에서 2역 이상**을 하도록 설계한다:
- **시간차 버튼**: 누르면 `holdTicks`만 열림. 잔상이 *정확한 순간*에 눌러야 한다 → 잔상은 "위치"가 아니라 "타이밍" 자원.
- **상자 릴레이**: 상자를 발판에 올리면 몸 하나를 아낀다. A가 중간까지 밀고 B가 이어받는다.
- **경비 미끼**: 잔상이 뛰어서(`SHIFT`) 소음을 내면 경비가 그쪽을 조사한다 → 잔상 = 유인 자원.
- **레버 토글**: 지나가며 켜고, 되돌아와 끈다 → 한 잔상이 전반/후반에 다른 역할.
- 각 스테이지의 par(§9)는 **잔상을 다역으로 쓸 때만** 달성 가능하게 설계한다.

---

# 4. 결정론 (Critical Divergence = 0)

PRD §19.2가 "핵심 mechanic이 무너진다"고 지목한 지점. 다음을 **강제**한다.

- **고정 틱 60Hz.** accumulator 방식, `MAX_STEPS_PER_FRAME = 5`(spiral of death 방지), 잔여 시간 이월.
- 시뮬 내부의 모든 위치·속도·타이머는 **정수**. 부동소수점 금지.
  - `SUBPIXEL = 256`. `TILE = 32px` → 1타일 = `8192` 서브픽셀 단위.
- 입력은 틱당 **1바이트 비트마스크**:
  `UP=1, DOWN=2, LEFT=4, RIGHT=8, INTERACT=16, RUN=32`
- 테이프 = `Uint8Array`. 재생 = 같은 바이트를 같은 순서로 주입 → **오차 정확히 0** (근사가 아님).
- 시뮬 내부에서 **금지**: `Math.random()`, `Date.now()`, `performance.now()`, `Math.sin/cos/sqrt`(부동소수),
  객체 키 순회 순서 의존, `Array.sort` 비안정 비교.
  - 거리 비교는 **제곱거리 정수 비교**로. 시야 판정은 정수 외적/내적으로.
- 엔티티는 **id 오름차순 고정 순서**로만 갱신.
- **틱 내 갱신 순서(고정)**:
  1. 입력 적용 (모든 바디, id 순)
  2. 바디 이동 + 벽/닫힌문 충돌 (축분리: X → Y)
  3. 상자 밀림 해결
  4. 소음 이벤트 발행
  5. 경비 AI 갱신 (감지 → 상태전이 → 이동)
  6. CCTV 갱신
  7. 장치(발판/버튼/레버) 평가 → 채널 → 게이트 반영
  8. loot 획득 판정
  9. 체포 판정
  10. 클리어 판정
- 렌더는 시뮬 상태를 **읽기만** 한다. 보간·파티클·셰이크는 렌더 전용 상태에 둔다(시뮬 오염 금지).

---

# 5. 시뮬레이션 규칙

### 5.1 바디(Body)
- AABB `24×24px` (= 6144 서브픽셀). 타일 32px.
- 걷기 `2.0px/tick` = `512 서브픽셀/tick`. 달리기(`SHIFT`) `3.25px/tick` = `832`.
- 대각선: 축별로 `WALK_DIAG = 362`, `RUN_DIAG = 588` (= floor(v/√2)를 상수로 **하드코딩**).
- 가속도 없음. 즉시 정지/출발 — 결정론과 예측 가능성 우선.
- **바디끼리 충돌하지 않는다**(PRD §6.3). 벽·닫힌 게이트·상자와만 충돌.
- 달리는 동안 매 12틱마다 반경 `160px`의 **소음 이벤트** 발행. 걷기는 무음.

### 5.2 상자(Crate)
- `32×32`. 바디가 밀면 미는 바디와 동일 속도로 밀린다. 벽/다른 상자에 막히면 정지하고 미는 바디도 그 축으로 정지.
- 여러 바디가 반대 방향으로 동시에 밀면 → 합력 0, 정지. 처리 순서는 id 오름차순.
- 상자는 발판을 누른다. 경비는 상자를 통과하지 못한다(경로 차단 = 전술 자원).

### 5.3 경비(Guard) — 결정론 상태기계
상태: `PATROL → SUSPICIOUS → INVESTIGATE → CHASE → RETURN → PATROL`

- **순찰**: `path[]` 웨이포인트 순환, `waitTicks` 만큼 각 지점 대기. 속도 `384`.
- **시야**: 정면 기준 반각 `50°`, 사거리 `224px`(7타일). 벽에 의해 차단(타일 레이캐스트, 정수 DDA).
- **감지 대상**: 살아있는 바디(I 및 잔상). **시체는 시야로 감지하지 않는다**(이미 처리된 것으로 간주)
  — 단, 시체는 **경로를 막지 않고** 발판만 누른다.
- **감지 게이지**: 시야 안에 있으면 틱당 `+3`(달리는 중이면 `+6`), 밖이면 `-2`. `0..100` 클램프.
  - `>= 40` → `SUSPICIOUS`(마지막 목격 지점 기록, 그쪽을 바라봄)
  - `= 100` → `CHASE`
- **CHASE**: 대상 위치로 직선 추격(속도 `512`). 대상과 겹치면 **체포**.
  - 대상이 잔상이면 → 그 잔상만 "발각" 처리(잔상은 계속 재생됨, 미끼 성립), 경비는 `RETURN`.
  - 대상이 **I** 이면 → 루프 종료(CAPTURE), 알람 카운트 +1.
- **소음**: 소음 이벤트가 반경 안이면 `INVESTIGATE`, 소음 지점으로 이동 후 `120틱` 탐색 → `RETURN`.
- **RETURN**: 가장 가까운 순찰 웨이포인트로 복귀 → `PATROL`.
- 경로 탐색은 **A\* 없이** 축정렬 이동 + 벽 슬라이딩(맵이 작아 충분). 완전 결정론.

### 5.4 CCTV
- 고정 위치, 시야 부채꼴이 `sweepTicks` 주기로 좌우 왕복(**결정론**, `phase` 오프셋 지원).
- 살아있는 바디를 `45틱` 연속 포착 → `ALERT`: 반경 전체 경비를 해당 지점으로 `CHASE` 전환, 알람 +1.
- `channel`에 연결된 레버로 **비활성화 가능**. 비활성 시 붉은 시야가 꺼진다(명확한 시각 피드백).

### 5.5 장치 / 채널
| 장치 | 동작 | 필드 |
|---|---|---|
| `plate` | 바디(시체 포함) 또는 상자가 겹친 동안 ON | `channel` |
| `button` | `INTERACT`로 누르면 `holdTicks` 동안 ON. 재누름 시 타이머 리셋 | `channel`, `holdTicks` |
| `lever` | `INTERACT`로 토글, 상태 유지 | `channel` |
| `gate` | 채널 ON이면 열림(통과 가능). `invert:true`면 반대 | `channel`, `invert?` |
| `loot` | I 또는 잔상이 겹치면 획득. **잔상이 들면 잔상이 운반**하며, 잔상 정지 시 그 자리에 놓임 | — |
| `escape` | **loot 보유한 I**가 겹치면 클리어 | — |

- 한 채널에 소스가 여러 개면 기본 **AND**(전부 ON), 스테이지 정의에서 `channelMode: 'OR'` 지정 가능.
- 상호작용 사거리 `40px`, 가장 가까운 대상 1개. 동률이면 **id 낮은 쪽**(결정론).

---

# 6. 조작 (PRD §7 준수)

| 키 | 동작 |
|---|---|
| `WASD` / 방향키 | 이동 |
| `Shift` | 달리기 (빠름, **소음 발생**) |
| `E` | 상호작용 (버튼 / 레버) |
| `R` | **루프 조기 확정** → 잔상화 |
| `Q` → `1/2/3` | 잔상 슬롯 덮어쓰기 (스테이지당 1회) |
| `Backspace` (2초 홀드) | 스테이지 전체 초기화 (`DEBT +1`) |
| `Esc` | 일시정지 / 조작법 |
| `Enter` | (클리어 화면) 다음 스테이지 |

---

# 7. 렌더 / 아트 디렉션

- 캔버스 내부 해상도 **960×600 고정**, CSS로 정수배 스케일. `imageSmoothingEnabled = false`.
- 외부 이미지 에셋 0개. 전부 코드 드로잉(도형 + 그라디언트 + 글로우).

### 팔레트
```
배경        #07080d      바닥 타일   #12152180 격자   #1a1f33
벽          #232a44 (상단 하이라이트 #2f3860)
I (현재)    코어 #f2f8ff  링 #6ce8ff   — 불투명, 항상 최상단 레이어
MY          #4fd8ff  α0.50
ME          #a97bff  α0.45
MINE        #ff6b9d  α0.40
시체        회색 #6d7590 α0.28 + 3초 주기 미세 플리커, 십자 마커
장치 ON     #7dffb0      OFF #3d445e
경비        #ff5a4d   시야콘 #ff5a4d α0.10 (SUSPICIOUS α0.16, CHASE α0.24 + 펄스)
CCTV        본체 #ff2fb0   스캔 빔 #c9e9ff α0.09 (LOCK 시 α0.12~0.16 펄스)
잔상 외곽선 #07080d α0.72 (시체는 ×0.6) — 1px, 몸통 바깥 한 줄
loot        #ffd75c (금색 글로우 + 회전 마름모)
escape      #7dffb0 (열림) / #3d445e (loot 미보유 시 잠김 표시)
경고/DEBT   #ff3b5c
```

### 가독성 규칙 (PRD Risk 3 대응)
- 잔상은 **항상 I보다 아래 레이어**. I에는 얇은 흰 링 + `I` 라벨 + 미세 펄스.
- 잔상마다 **4프레임 모션 트레일**(α 0.06씩 감쇠) — 움직이는 잔상과 정지한 잔상이 즉시 구분된다.
- 잔상 몸에 **1px 배경색 외곽선**(α0.72) — 채움은 반투명 그대로라 "과거" 느낌은 유지하면서
  밝은 스캔 빔·붉은 시야콘·다른 잔상 위 어디서도 실루엣이 끊긴다.
- 위험 영역 3종은 서로 다른 색역을 쓴다: 간수 순찰콘(따뜻한 적색, 옅음) / 간수 추격콘(적색 + 펄스) /
  EYE 스캔 빔(차가운 저채도 흰빛). 잔상 4색 어느 것과도 겹치지 않는다.
- 잔상 머리 위 슬롯 라벨(`MY`/`ME`/`MINE`) 8px + 어두운 헤일로. 라벨끼리 또는 몸과 겹치면
  9px씩 **위로만** 밀어 피하고(최대 3회, 밀린 라벨은 1px 연결선으로 주인 표시),
  그래도 자리가 없으면 α×0.3 으로 흐려 맨 앞 한 장만 남긴다. 라벨은 몸을 덮지 않는다.
- 게이트 개폐 3틱 보간, 체포 시 8프레임 화면 흔들림, 획득 시 방사 링 — **전부 렌더 전용 상태**.

### 사운드 (PRD §16 — WebAudio 절차 생성, 에셋 0)
`footstep`(달릴 때만), `interact`, `door`, `alert`(경비 발각 시 하강 2음), `loot`(상승 아르페지오),
`commit`(잔상 확정 — 리버스 스윕), `capture`, `clear`(4음 팡파레). 마스터 볼륨 `M`으로 뮤트 가능.

---

# 8. HUD / 화면

- **상단 좌**: `STAGE 1 — THE CELL` / 그 아래 `PAR 1`
- **상단 중앙**: 60초 틱 게이지 바 + `R ▸ COMMIT` 힌트
- **상단 우**: 슬롯 인디케이터 `[I][MY][ME][MINE]` — 채워진 슬롯 점등, 시체 슬롯은 `✕`
- **하단 좌**: `OVERWRITE ●`(남음) / `○`(소진)
- **하단 우**: `DEBT n` (n>0이면 붉게), `ALERTS n`
- **루프 전환 오버레이** (≤ 2초, PRD §15 카피 준수):
  ```
  CAPTURED.
  YOUR ACTIONS WILL RETURN.
  LOOP 2 — YOU ARE "ME"
  ```
- **클리어 오버레이**: `HEIST COMPLETE` / `LOOPS n` / `TIME mm:ss.ff` / `ALERTS n` /
  par 달성 시 `MINIMUM AFTERIMAGE ★` / 누적 `DEBT n` / `ENTER ▸ NEXT`
- **타이틀**: `I.MY.ME.MINE` + 태그라인 `Fail now. Steal later.` + `[ PLAY ]` — 클릭/Enter 즉시 시작.
  로그인·긴 메뉴 없음(PRD §8.1).

---

# 9. 스테이지 5종 (수제)

`par` = 메달 기준 잔상 수. 사용 잔상 수 ≤ par → **MINIMUM AFTERIMAGE ★**.
레벨은 **순수 데이터**(`levels.ts`). 타일맵은 문자열 배열(`#`벽 `.`바닥 `S`스폰), 장치는 좌표 배열.

| # | id / 이름 | 신규 요소 | 구성 | par | 교육 목표 |
|---|---|---|---|---|---|
| 1 | `01_I` **THE CELL** | plate, gate, loot, escape | 발판1 · 게이트1 · loot · escape | 1 | **코어 아하.** 발판을 밟은 채 `R` 확정 → 잔상이 눌러주는 문으로 내가 통과. Time-to-Aha < 30초. |
| 2 | `02_MY` **THE CORRIDOR** | guard, run/noise | 경비1(순찰) · 시간차버튼(`holdTicks 180`) · 게이트 · loot · escape | 2 | 경비 시야 + **소음 미끼**. 잔상이 달려서 경비를 반대편으로 끌고, 다른 잔상이 버튼. |
| 3 | `03_ME` **THE WEIGHT** | crate | 발판2(AND) · 상자1 · 경비1 · loot · escape | 1 | **몸은 유한하다, 세계를 써라.** 순진하게 풀면 잔상 2, 상자를 발판에 올리면 잔상 1. |
| 4 | `04_MINE` **THE VAULT** | CCTV, lever | 경비1 · CCTV1 · 레버(CCTV 차단) · 발판1 · 시간차버튼 · loot · escape | 2 | 레버로 CCTV 무력화. 하나의 잔상이 레버→발판 2역. |
| 5 | `05_MINE_FINAL` **THE GALLERY** | 복합 | 경비2 · CCTV1 · 레버 · 상자1 · 발판1 · 시간차버튼(`holdTicks 150`) · loot · escape | 3 | 4바디 총동원. par 3 = 상자 릴레이 + 미끼를 겸하는 최적 루트를 찾아야 달성. |

> 스테이지 1~3은 작고 빠르다(각 1~2분). 4~5가 PRD가 말한 "one polished heist"의 본체다.
> 3분 데모 루트(PRD §31)는 **1번(아하) → 5번(와우)** 로 구성한다.

---

# 10. 완료 기준 (검증하고 증거와 함께 보고할 것)

**아래 4개를 실제로 실행해 통과 출력을 확인하기 전까지 "완료"라고 보고하지 말 것.**

1. `npm run build` — TypeScript `strict: true`, 오류/경고 0.
2. `npm test` (`node --test`, ts는 사전 빌드 또는 tsx) — 두 스위트 전부 통과:
   - **`tests/determinism.test.ts`**: 무작위 입력 테이프를 100회 생성해 각각 2번씩 시뮬 → 최종 상태 해시(FNV-1a, 모든 엔티티 좌표/상태/타이머 포함)가 **완전히 동일**. 불일치 0.
   - **`tests/solvability.test.ts`**: **5개 스테이지 각각**에 대해 하드코딩된 정답 입력 테이프 세트를
     헤드리스 시뮬에 넣어 `cleared === true` **그리고** 사용 잔상 수 `<= par` 확인.
     → 이 테스트가 통과하지 않는 스테이지는 **미완성이다. 레벨 좌표를 고쳐서 통과시킬 것.**
3. 브라우저 실기동 확인: 타이틀 → 스테이지1에서 이동 → `R` 확정 → 잔상 재생 → 게이트 통과 → loot → escape → 클리어 오버레이.
4. `README.md`: 컨셉 / 조작 / 실행법 / 결정론 아키텍처 설명 / 테스트 실행법 / 심사용 3분 데모 루트.

---

# 11. 파일 구조

```
imymemine/
  index.html
  package.json         vite, typescript, tsx(테스트용)만. 런타임 의존성 0.
  tsconfig.json        strict: true, noUncheckedIndexedAccess: true
  vite.config.ts       base: './'
  README.md
  SPEC.md              (이 문서)
  src/
    main.ts            부트스트랩, 씬 전환(TITLE / PLAY / CLEAR)
    engine/
      loop.ts          accumulator 고정 틱
      input.ts         키보드 → InputMask (엣지 트리거 관리)
      tape.ts          Tape 녹화/재생/확정/덮어쓰기
      audio.ts         WebAudio 절차 생성 SFX
    sim/
      constants.ts     SUBPIXEL/TILE/속도/틱 상수 전부 여기 (매직넘버 금지)
      types.ts         SimState, Body, Crate, Guard, Device, LevelDef, InputMask
      world.ts         createWorld(level, tapes) / stepWorld(state) — 순수 함수, DOM 무접근
      physics.ts       AABB, 축분리 충돌, 상자 밀기, 정수 레이캐스트
      guard.ts         경비 상태기계
      devices.ts       채널 평가, 발판/버튼/레버/게이트/CCTV
      hash.ts          상태 해시 (FNV-1a, 결정론 테스트용)
    game/
      session.ts       루프 진행, commit/overwrite/reset, DEBT, 메달, 알람
      levels.ts        5개 스테이지 데이터
    render/
      palette.ts
      renderer.ts      월드 드로우 (트레일/글로우/셰이크)
      hud.ts           HUD + 오버레이 + 타이틀
  tests/
    determinism.test.ts
    solvability.test.ts
```

`sim/` 은 **DOM·브라우저 API에 절대 접근하지 않는다.** 그래야 헤드리스 테스트가 가능하다.

---

# 12. 금지 사항

- 미구현 스텁, `TODO` 주석, `test.skip`/`test.only`, placeholder 레벨 — **전부 블로커**.
- 시뮬 내부의 부동소수점 / `Math.random()` / `Date.now()` / DOM 접근.
- 외부 런타임 의존성 추가(빌드·테스트 도구 제외).
- 외부 이미지·오디오 에셋 파일.
- 런타임 네트워크 호출 일체.
