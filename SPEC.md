# I.MY.ME.MINE — 구현 명세 v1.1 (확정본)

> 실패할 때마다 과거의 내가 동료가 되는 60초 타임루프 **탈출극**.
> **Fail now. Escape later.**

이 문서는 `../afterimage_prd_draft_v0.1.md`(PRD v0.1)를 상위 문서로 하고,
구현 가능한 수준으로 결정을 못 박은 **개발 명세**다. PRD의 §33 Open Questions는 여기서 전부 닫는다.

> **v1.1 (현재 구현 반영)** — 게임은 **하이스트에서 탈출극으로 재구성**됐다. 훔치는 이야기가
> 아니라 **몸에서 빼앗긴 억제 코어를 되찾아 시설을 빠져나가는** 이야기다. 그에 따라 태그라인은
> `Fail now. Escape later.`, 승리 카피는 `ESCAPED` / `ALL ESCAPED` 다.
> 스테이지는 5개에서 **15개(4막)** 로, 장치는 **grate · seqButton · laser · powerBus** 4종이 추가됐고,
> 플레이 방식 축(`STORY` / `GAUNTLET` / `TIME ATTACK`)과 감지 방식 축(`EASY` / `LISTEN` — 마이크)이
> 서로 직교하게 붙었다. 아래 본문은 그 상태를 기술한다.

---

## 0. 제출 컨텍스트 / 제약

- OpenAI Game Builders Seoul 2026 제출용.
- **게임 런타임에 OpenAI API 및 일체의 네트워크 호출을 사용하지 않는다.** 100% 로컬/정적.
  - AI 활용의 증거는 런타임이 아니라 **개발 프로세스**(자동 결정론 회귀 테스트 + 레벨 solvability 증명)로 제시한다. PRD §20–22와 일치.
- 스택: **TypeScript + Vite + Canvas 2D**. 런타임 의존성 0개 (Phaser/Pixi 미사용 — 결정론 통제를 위해 직접 루프).
- 심사위원이 **설치 없이 링크로 즉시 플레이**: <https://nuounce.github.io/imymemine/>
  (GitHub Pages 자동 배포 — §10-5). 로컬은 `npm i && npm run dev`. 빌드 결과는 정적 파일(`base: './'`).
- 데스크톱 Chrome 우선, 60FPS.

## 0.1 PRD와의 차이 (의도적 결정)

| 항목 | PRD v0.1 | 본 명세 | 이유 |
|---|---|---|---|
| 제목 | AFTERIMAGE | **I.MY.ME.MINE** | AFTERIMAGE는 기출시작과 충돌. 네 격이 곧 몸 슬롯이 되어 제목=시스템. |
| 장르 프레이밍 | 하이스트 (`Fail now. Steal later.`) | **탈출극** (`Fail now. Escape later.`) | 훔치는 동기는 몸에 박힌 억제 코어를 되찾는 동기보다 약하다. 잔상을 남기는 능력의 출처가 곧 감금의 이유가 되어 세계관과 시스템이 한 문장으로 붙는다. 승리 카피도 `ESCAPED`. |
| 최대 잔상 | 5 | **3** (총 4바디) | PRD §11 예시 플레이가 정확히 4루프. 5는 화면 혼잡(Risk 3)만 키움. |
| 스택 | Phaser/Pixi 후보 | Canvas 2D 직접 | 결정론(§4)을 프레임워크에 위임할 수 없음. |
| 루프 종료 | 60초 or 실패 | **+ 언제든 조기 확정** | PRD가 안 다룬 대기시간 문제 해결. §3-1. |
| 리셋 | Open Question | **덮어쓰기 1회 + 유료 초기화** | PRD §33 "Manual Reset 허용 여부"의 답. §3-2. |
| 레벨 수 | 1 heist | **15** (4막 — 수용 4 / 감시 4 / 통제 4 / 경계 3) | 3분 데모 루트(PRD §31)에 난이도 곡선이 필요하고, 막마다 새 규칙 하나씩이라야 15개가 반복이 되지 않는다. §9. |
| 장치 | 발판/버튼/레버/게이트/경비/CCTV | **+ grate · seqButton · laser · powerBus** | 2~4막의 "새 규칙 하나"를 담당한다. 각각 소음 경로 선택 · 되돌릴 수 없는 순서 · 예측 가능한 위험 · 강제 트레이드오프. §5.6. |
| 모드 | 없음 | **플레이 방식 3종 × 감지 방식 2종** (직교) | 진행(STORY) / 부채 누적(GAUNTLET) / 총 시간(TIME ATTACK) 은 성적 기준이고, EASY/LISTEN 은 입력 채널이다. 두 축은 섞이지 않는다. §12. |
| 입력 | 키보드 | **+ 마이크 레벨 2비트(LISTEN)** | 실시간 소리를 시뮬에 직접 넣지 않고 **테이프에 녹화**해 결정론을 지킨다. §4.1. |

---

# 1. 코어 루프

플레이어는 항상 **한 명**을 조작한다. 조작 중 모든 입력은 고정 틱으로 녹화된다.
녹화가 확정되면(조기 확정 / 사망 / 시간초과) 그 런은 **잔상(afterimage)** 이 되고,
**스테이지 전체가 틱 0으로 리셋된 뒤** 모든 잔상이 동시에 재생되는 위에서 새 몸을 조작한다.

```
LOOP N 시작 → 월드를 틱 0으로 리셋
           → 잔상 1..N-1을 각자의 테이프로 재생 (동시)
           → 플레이어는 새 몸 조작 (동시)
           → 확정 → LOOP N+1
```

> **중요 아키텍처**: 잔상은 "저장된 궤적"이 아니라 **저장된 입력 테이프**다.
> 매 루프 월드를 처음부터 다시 시뮬레이션하며, 잔상에는 녹화된 입력 비트마스크를 먹인다.
> 따라서 경비병은 이번 루프에 존재하는 **모든** 몸(잔상 포함)에 반응하고,
> 그 반응까지 완전히 결정론적이다. 이게 "과거를 준비한다"는 PRD §36 원칙의 기계적 구현이다.

## 1.1 네 개의 몸 = 제목

| 슬롯 | 의미 | 상태 |
|---|---|---|
| **I** | 지금 조작 중인 나 | 항상 1명, 불투명 |
| **MY** | 첫 번째 확정된 나 | 잔상 |
| **ME** | 두 번째 | 잔상 |
| **MINE** | 세 번째 | 잔상 |

- 총 4바디 상한. 모든 스테이지는 4바디 이내로 클리어 가능해야 한다(§10에서 프로그램으로 증명).
- 잔상 3개가 찬 상태(= 4번째 몸)에서 또 확정되면 → **LOOP FAILED**, 전체 초기화 강제 + `DEBT +1`.
  - 이 순간의 카피: `NO ONE LEFT TO BECOME.`

---

# 2. 승리 / 패배

- **승리**: `loot` 획득 **AND** 조작 중인 몸(I)이 `escape` 존 도달. 잔상의 탈출은 무효(PRD §17).
- **루프 종료(= 잔상 확정)** 조건 3가지:
  1. `R` 조기 확정 (플레이어 의지)
  2. 경비에게 **체포**(CAPTURE) — 잔상은 그 자리에 **시체**로 정지
  3. 3600틱(60초) 소진 — 마지막 위치에 정지
- 게임 오버는 없다. 4바디를 다 쓰면 초기화될 뿐이다(DEBT 대가).

---

# 3. 세 가지 개선 (반드시 구현 — 이게 차별점이다)

### 3-1. 조기 확정 — 대기시간 제거
- 60초는 **최대치일 뿐**. `R` 로 언제든 즉시 확정.
- 잔상은 자기 테이프 길이만큼 재생하고 **그 이후 마지막 틱 상태로 정지한다**(사라지지 않음).
  - → 5초만 녹화해 발판 위에 서서 확정하면, 그 잔상은 남은 55초 내내 발판을 눌러준다.
  - → 조기 확정이 **전략**이 된다. 이게 없으면 플레이어는 매번 60초를 기다린다.
- HUD에 `R ▸ COMMIT` 상시 노출. 확정→다음 루프 전환 **2초 이내**(PRD §15).

### 3-2. 실패 누적 / 덮어쓰기 1회 / 유료 초기화
- 실패(체포·시간초과)한 런도 **자동으로 잔상이 된다. 버릴 수 없다.**
  - 체포된 잔상 = **시체**. 여전히 발판을 누르고, 여전히 경비의 시야를 끈다(미끼로 유효).
  - "실패한 나는 동료이자 지울 수 없는 부채."
- `Q` → 덮어쓰기 모드 → `1/2/3` 으로 잔상 슬롯 선택 → 그 슬롯을 지우고 그 자리에서 재녹화.
  **스테이지당 정확히 1회.** 소진 시 HUD 배지가 `○`로 바뀐다.
- `BACKSPACE` **2초 홀드** → 스테이지 전체 초기화. 언제든 가능. **`DEBT +1`.**
  - `DEBT`는 스테이지를 넘어가도 **절대 리셋되지 않는다**(세션 누적, 엔딩 화면에 표시).
  - → 실수를 공짜로 지울 수는 없다.

### 3-3. 하나의 잔상이 여러 역할 — "발판 숫자 맞히기" 탈출
PRD Risk 1(`Button → Door → Button → Door` 정답 퍼즐화)에 대한 구체적 대응.
장치는 잔상 하나가 **시간축 위에서 2역 이상**을 하도록 설계한다:
- **시간차 버튼**: 누르면 `holdTicks`만 열림. 잔상이 *정확한 순간*에 눌러야 한다 → 잔상은 "위치"가 아니라 "타이밍" 자원.
- **상자 릴레이**: 상자를 발판에 올리면 몸 하나를 아낀다. A가 중간까지 밀고 B가 이어받는다.
- **경비 미끼**: 잔상이 뛰어서(`SHIFT`) 소음을 내면 경비가 그쪽을 조사한다 → 잔상 = 유인 자원.
- **레버 토글**: 지나가며 켜고, 되돌아와 끈다 → 한 잔상이 전반/후반에 다른 역할.
- 각 스테이지의 par(§9)는 **잔상을 다역으로 쓸 때만** 달성 가능하게 설계한다.

---

# 4. 결정론 (Critical Divergence = 0)

PRD §19.2가 "핵심 mechanic이 무너진다"고 지목한 지점. 다음을 **강제**한다.

- **고정 틱 60Hz.** accumulator 방식, `MAX_STEPS_PER_FRAME = 5`(spiral of death 방지), 잔여 시간 이월.
- 시뮬 내부의 모든 위치·속도·타이머는 **정수**. 부동소수점 금지.
  - `SUBPIXEL = 256`. `TILE = 32px` → 1타일 = `8192` 서브픽셀 단위.
- **1타일 = 8192 서브픽셀, 걷기 = 512/틱 → 한 타일이 정확히 16틱.** 정답 테이프를 틱 단위로
  손으로 계산할 수 있어야 하므로 이 등식은 불변식이다(코너 어시스트도 이것을 깨지 않는다 — §5.1).
- 입력은 틱당 **1바이트 비트마스크**:
  `UP=1, DOWN=2, LEFT=4, RIGHT=8, INTERACT=16, RUN=32`, **비트 6~7 = 마이크 레벨 0~3**(§4.1)
- 테이프 = `Uint8Array`. 재생 = 같은 바이트를 같은 순서로 주입 → **오차 정확히 0** (근사가 아님).
- 시뮬 내부에서 **금지**: `Math.random()`, `Date.now()`, `performance.now()`, `Math.sin/cos/sqrt`(부동소수),
  객체 키 순회 순서 의존, `Array.sort` 비안정 비교.
  - 거리 비교는 **제곱거리 정수 비교**로. 시야 판정은 정수 외적/내적으로.
- 엔티티는 **id 오름차순 고정 순서**로만 갱신.
- **틱 내 갱신 순서(고정)**:
  1. 입력 적용 (모든 바디, id 순)
  2. 바디 이동 + 벽/닫힌문 충돌 (축분리: X → Y) + 코너 어시스트
  3. 상자 밀림 해결 (2와 같은 패스에서 해결된다)
  4. 소음 이벤트 발행 — **달리기 / 마이크 / 소음 바닥이 같은 경로를 탄다.**
     한 몸은 한 틱에 최대 **한 개**의 소음만 낸다(간격은 짧은 쪽, 반경은 큰 쪽으로 합침)
  5. 경비 AI 갱신 (감지 → 상태전이 → 이동)
  6. CCTV 갱신
  7. 장치(발판/버튼/레버/순차버튼) 평가 → 채널 → **전력 버스 중재** → 게이트/CCTV/레이저 전원 반영
  8. loot 획득 판정
  9. 체포 판정 — 경비와 **레이저**를 같은 지점에서 같은 규칙으로 판정한다
  10. 클리어 판정 (잔상의 탈출은 무효)
- 렌더는 시뮬 상태를 **읽기만** 한다. 보간·파티클·셰이크는 렌더 전용 상태에 둔다(시뮬 오염 금지).
- 힌트/속말(§13)도 **읽기 전용**이다. `SimState`·`Session` 의 어떤 필드도 쓰지 않는다.

## 4.1 마이크(LISTEN)와 결정론

실시간 마이크를 시뮬에 직접 먹이면 루프마다 소리가 달라져 **잔상이 매번 다르게 행동한다.**
그래서 그렇게 하지 않는다.

- 조작 중인 몸(I)의 마이크 레벨(0~3)을 매 틱 입력 마스크의 **비트 6~7** 에 OR 해
  **테이프에 녹화**한다(`main.ts`). 잔상은 실시간 소리가 아니라 **녹화된 비트**를 재생한다.
  실시간 마이크가 잔상에 닿는 경로는 존재하지 않는다 → 재생 오차 **정확히 0** 유지.
- `src/engine/mic.ts` 는 **시뮬 밖**이다. `src/sim/` 은 `AudioContext`·`navigator`·DOM 을
  한 번도 참조하지 않는다(헤드리스 테스트 전제).
- 마이크 레벨별 소음 반경: 1 = 96px, 2 = 176px, 3 = 272px (달리기 160px, 격자 128px).
- **EASY 에서는 이 비트가 항상 0** 이라 마이크가 없는 것과 완전히 동일하다.
- 캘리브레이션: 시작 시 120틱(2초) 환경음을 재고 모든 임계를 **바닥값 대비 상대치**로 잡는다.
  감도 7단계. AGC·노이즈 억제·에코 제거는 끈다(켜면 모드의 전제가 사라진다).
- 실패(권한 거부·미지원·장치 오류·30초 무응답) 시 **EASY 로 폴백**하고 사유를 한 줄 표시한다.
  게임이 멈추는 경로는 없다.
- **개인정보**: 오디오를 저장·전송하지 않는다. RMS 는 그 자리에서 0~3 정수가 되고 버려지며,
  테이프에 남는 것은 **틱당 2비트**뿐이다. 모드 이탈 시 모든 트랙을 `stop()` 으로 반납한다.

---

# 5. 시뮬레이션 규칙

### 5.1 바디(Body)
- AABB `24×24px` (= 6144 서브픽셀). 타일 32px.
- 걷기 `2.0px/tick` = `512 서브픽셀/tick`. 달리기(`SHIFT`) `3.25px/tick` = `832`.
- 대각선: 축별로 `WALK_DIAG = 362`, `RUN_DIAG = 588` (= floor(v/√2)를 상수로 **하드코딩**).
- 가속도 없음. 즉시 정지/출발 — 결정론과 예측 가능성 우선.
- **바디끼리 충돌하지 않는다**(PRD §6.3). 벽·닫힌 게이트·상자와만 충돌.
- 달리는 동안 매 12틱마다 반경 `160px`의 **소음 이벤트** 발행. 걷기는 무음.
  단 **소음 바닥(grate) 위에서는 걷기도 무음이 아니다**(§5.5). 서 있으면 무음.
- **코너 어시스트**: 몸 `24px` 이 1타일 통로 `32px` 에 들어가면 좌우 여유가 각 `4px` 뿐이라,
  정렬하지 못하면 축분리 충돌이 그 축을 0으로 만들어 "사각형에 맞춰 지나는" 느낌을 준다.
  그래서 **막혔을 때만** 수직축을 밀어 모서리를 돌게 한다(`physics.ts` `moveBody`):
  - 걸친 양이 `CORNER_ASSIST_MAX = 8px` 이하일 때만. 넘으면 벽을 정면으로 미는 상황이므로 무보정.
  - 1틱당 `CORNER_ASSIST_SPEED = 192`(0.75px). 어시스트 경로에 닫힌 게이트/상자가 있으면 취소.
  - **속도 상수(WALK/RUN/DIAG)는 손대지 않는다.** 어시스트는 가속이 아니라 막힘 해소이므로
    **직선 주행은 여전히 1타일 = 정확히 16틱**이고, 기존 정답 테이프는 무효화되지 않는다.
    조작 몸과 잔상은 같은 코드 경로를 타므로 궤적이 완전히 일치한다(`tests/movement.test.ts`).

### 5.2 상자(Crate)
- `32×32`. 바디가 밀면 미는 바디와 동일 속도로 밀린다. 벽/다른 상자에 막히면 정지하고 미는 바디도 그 축으로 정지.
- 여러 바디가 반대 방향으로 동시에 밀면 → 합력 0, 정지. 처리 순서는 id 오름차순.
- 상자는 발판을 누른다. 경비는 상자를 통과하지 못한다(경로 차단 = 전술 자원).

### 5.3 경비(Guard) — 결정론 상태기계
상태: `PATROL → SUSPICIOUS → INVESTIGATE → CHASE → RETURN → PATROL`

- **순찰**: `path[]` 웨이포인트 순환, `waitTicks` 만큼 각 지점 대기. 속도 `384`.
- **시야**: 정면 기준 반각 `50°`, 사거리 `224px`(7타일). 벽에 의해 차단(타일 레이캐스트, 정수 DDA).
- **감지 대상**: 살아있는 바디(I 및 잔상). **시체는 시야로 감지하지 않는다**(이미 처리된 것으로 간주)
  — 단, 시체는 **경로를 막지 않고** 발판만 누른다.
- **감지 게이지**: 시야 안에 있으면 틱당 `+3`(달리는 중이면 `+6`), 밖이면 `-2`. `0..100` 클램프.
  - `>= 40` → `SUSPICIOUS`(마지막 목격 지점 기록, 그쪽을 바라봄)
  - `= 100` → `CHASE`
- **CHASE**: 대상 위치로 직선 추격(속도 `512`). 대상과 겹치면 **체포**.
  - 대상이 잔상이면 → 그 잔상만 "발각" 처리(잔상은 계속 재생됨, 미끼 성립), 경비는 `RETURN`.
  - 대상이 **I** 이면 → 루프 종료(CAPTURE), 알람 카운트 +1.
- **소음**: 소음 이벤트가 반경 안이면 `INVESTIGATE`, 소음 지점으로 이동 후 `120틱` 탐색 → `RETURN`.
- **RETURN**: 가장 가까운 순찰 웨이포인트로 복귀 → `PATROL`.
- 경로 탐색은 **A\* 없이** 축정렬 이동 + 벽 슬라이딩(맵이 작아 충분). 완전 결정론.

### 5.4 CCTV
- 고정 위치, 시야 부채꼴이 `sweepTicks` 주기로 좌우 왕복(**결정론**, `phase` 오프셋 지원).
- 살아있는 바디를 `45틱` 연속 포착 → `ALERT`: 반경 전체 경비를 해당 지점으로 `CHASE` 전환, 알람 +1.
- `channel`에 연결된 레버로 **비활성화 가능**. 비활성 시 붉은 시야가 꺼진다(명확한 시각 피드백).

### 5.5 장치 / 채널
| 장치 | 동작 | 필드 |
|---|---|---|
| `plate` | 바디(시체 포함) 또는 상자가 겹친 동안 ON | `channel` |
| `button` | `INTERACT`로 누르면 `holdTicks` 동안 ON. 재누름 시 타이머 리셋 | `channel`, `holdTicks` |
| `lever` | `INTERACT`로 토글, 상태 유지 | `channel` |
| `gate` | 채널 ON이면 열림(통과 가능). `invert:true`면 반대 | `channel`, `invert?` |
| `loot` | I 또는 잔상이 겹치면 획득. **잔상이 들면 잔상이 운반**하며, 잔상 정지 시 그 자리에 놓임 | — |
| `escape` | **loot 보유한 I**가 겹치면 클리어 | — |

- 한 채널에 소스가 여러 개면 기본 **AND**(전부 ON), 스테이지 정의에서 `channelMode: 'OR'` 지정 가능.
- 소스가 하나도 없는 채널은 항상 OFF (빈 AND 를 참으로 보지 않는다).
- 상호작용 사거리 `40px`, 가장 가까운 대상 1개. 동률이면 **id 낮은 쪽**(결정론).
- 레버는 `LEVER_COOLDOWN_TICKS = 10` 동안 잠긴다 — 여러 몸이 한 틱에 연타하지 못하게.

### 5.6 확장 장치 4종 (2~4막의 "새 규칙 하나")

| 장치 | 동작 | 필드 | 결정론 근거 |
|---|---|---|---|
| `grate` (소음 바닥) | 이 사각형 위에서 **실제로 변위가 있으면** `10틱`마다 반경 `128px` 소음. 서 있으면 무음. 걷기도 무음이 아니다 | `tx, ty, w, h` | 상태 없는 순수 질의(`rectOnGrate`). 소음 발행은 §4 4단계에서 달리기·마이크와 한 지점에서 병합 |
| `seqButton` (순차 버튼) | 그룹 안에서 `order` 순으로만 진행. **틀리면 그룹이 0으로 리셋**(지나간 order 재입력도 오류). 완성된 그룹은 영구 ON(그룹명 = 채널명) | `group, order` | 그룹은 이름 오름차순, order 는 숫자 오름차순으로 정렬해 순회 순서를 못박는다 |
| `laser` (주기 레이저) | `periodTicks` 주기로 `onTicks` 만큼 ON. 켜진 빔에 살아있는 바디가 닿으면 **경비 체포와 동일 처리**(I → 루프 종료 + 알람, 잔상 → 발각만, 시체 → 무판정). `disableChannel` 로 차단 가능 | `from, to, periodTicks, onTicks, disableChannel?` | ON/OFF 는 틱 위상만으로 결정. 선분 × AABB 는 완전 정수 SAT(박스 X·Y + 선분 법선 3축) |
| `powerBus` (전력 채널) | 한 버스에서 동시에 ON 일 수 있는 채널은 **하나**. 이번 틱에 **새로** 켜진 채널이 전력을 가져가고, 아무도 새로 켜지지 않으면 쥐고 있던 채널이 유지된다 | `bus, channels[]` | 동시에 새로 켜지면 `channels` 배열의 **앞선 인덱스**가 이긴다. `prevOn` 에는 강제 OFF 결과가 아니라 **원래 소스 요청**을 기록해 밀려난 채널이 전력을 되뺏지 않는다 |

- 채널 확정(§4 7단계) 순서는 **소스 평가 → 버스 중재 → 싱크 반영**이다. 그래서 게이트·CCTV·레이저의
  `disableChannel` 은 항상 **중재를 거친** 값을 본다.

---

# 6. 조작 (PRD §7 준수)

| 키 | 동작 |
|---|---|
| `WASD` / 방향키 | 이동 |
| `Shift` | 달리기 (빠름, **소음 발생**) |
| `E` | 상호작용 (버튼 / 레버) |
| `R` | **루프 조기 확정** → 잔상화 |
| `Q` → `1/2/3` | 잔상 슬롯 덮어쓰기 (스테이지당 1회) |
| `Backspace` (2초 홀드) | 스테이지 전체 초기화 (`DEBT +1`) |
| `Esc` | 일시정지 / 조작법 |
| `Enter` | 타이틀 시작 / (클리어 화면) 다음 스테이지 / (결과 화면) 타이틀 |
| `M` | 뮤트 |
| `↑` / `↓` | (타이틀) 축 이동 — 플레이 방식 ↔ 감지 방식 |
| `←` / `→`, `1`/`2`/`3` | (타이틀) 포커스된 축의 항목 선택 |
| `S` | (타이틀) 인트로 만화 다시 보기 |
| `−` / `=` | 마이크 감도 −/+ (LISTEN) |

- **테이프에 녹화되는 것은 이동·상호작용·달리기 비트뿐이다**(+ 마이크 2비트, §4.1).
  `R`·`Q`·`1/2/3`·`Backspace`·`Esc`·`Enter`·`M` 은 세션을 조작하는 **메타키**이고 마스크에 섞이면
  안 된다 — 섞이면 재생 중인 잔상이 루프를 확정하려 든다. 메타키는 별도 엣지 큐로만 노출한다.
- 타이틀은 캔버스 클릭만으로도 시작된다(로그인·긴 메뉴 없음). 인트로 중에는 **아무 키나** 다음
  칸으로 간다(모바일은 화면 탭). 마지막 칸 뒤에서만 타이틀로 넘어간다.
- `getUserMedia` 는 사용자 제스처 안에서만 열 수 있으므로, 마이크는 `Enter`/`pointerdown`
  핸들러에서 연다(rAF 콜백은 제스처 컨텍스트가 아니다).

---

# 7. 렌더 / 아트 디렉션

- 캔버스 내부 해상도 **960×600 고정**, CSS로 정수배 스케일. `imageSmoothingEnabled = false`.
- 외부 이미지 에셋 0개. 전부 코드 드로잉(도형 + 그라디언트 + 글로우).

### 팔레트

**팔레트의 단일 진실은 `src/render/palette.ts` 다** — 색값을 이 문서에 복제하지 않는다(복제하면
반드시 어긋난다). 지켜야 할 것은 값이 아니라 아래 관계다:

- 배경 → 바닥 → 벽이 명도로 3단 분리되고, **네 몸(I / MY / ME / MINE)** 은 서로 다른 색상환에서
  뽑는다. `I` 만 불투명 + 최상단, 잔상은 알파로 뒤로 물러난다. 시체는 채도를 빼고 십자 마커.
- **위험 3종은 서로 다른 색역을 쓴다**: 간수 순찰콘(따뜻한 적색, 옅음) / 간수 추격콘(적색 + 펄스) /
  EYE 스캔 빔(차가운 저채도 흰빛) / 레이저(경고색 + 켜짐 순간만 강조). 잔상 4색과 겹치지 않는다.
- 장치는 ON/OFF 두 색만 쓴다. loot 는 금색, escape 는 열림/잠김이 색으로 구분된다.
- 외부 이미지·폰트 에셋 0개. 전부 코드 드로잉(도형 + 그라디언트 + 글로우).

### 가독성 규칙 (PRD Risk 3 대응)
- 잔상은 **항상 I보다 아래 레이어**. I에는 얇은 흰 링 + `I` 라벨 + 미세 펄스.
- 잔상마다 **모션 트레일**(뒤로 갈수록 감쇠) — 움직이는 잔상과 정지한 잔상이 즉시 구분된다.
- 잔상 몸에 **1px 배경색 외곽선** — 채움은 반투명 그대로라 "과거" 느낌은 유지하면서
  밝은 스캔 빔·붉은 시야콘·다른 잔상 위 어디서도 실루엣이 끊긴다.
- 위험 영역은 서로 다른 색역을 쓴다: 간수 순찰콘(따뜻한 적색, 옅음) / 간수 추격콘(적색 + 펄스) /
  EYE 스캔 빔(차가운 저채도 흰빛) / 레이저(켜진 순간만 강조). 잔상 4색 어느 것과도 겹치지 않는다.
- (구체적 알파·프레임 수는 `render/palette.ts` 와 `renderer.ts` 가 단일 진실이다.)
- 잔상 머리 위 슬롯 라벨(`MY`/`ME`/`MINE`) 8px + 어두운 헤일로. 라벨끼리 또는 몸과 겹치면
  9px씩 **위로만** 밀어 피하고(최대 3회, 밀린 라벨은 1px 연결선으로 주인 표시),
  그래도 자리가 없으면 α×0.3 으로 흐려 맨 앞 한 장만 남긴다. 라벨은 몸을 덮지 않는다.
- 게이트 개폐 3틱 보간, 체포 시 8프레임 화면 흔들림, 획득 시 방사 링 — **전부 렌더 전용 상태**.

### 사운드 (PRD §16 — WebAudio 절차 생성, 에셋 0)
`footstep`(달릴 때만), `interact`, `door`, `alert`(경비 발각 시 하강 2음), `loot`(상승 아르페지오),
`commit`(잔상 확정 — 리버스 스윕), `capture`, `clear`(4음 팡파레). 마스터 볼륨 `M`으로 뮤트 가능.

---

# 8. HUD / 화면

- **상단 좌**: `STAGE 1 — THE CELL` / 그 아래 `PAR 1`
- **상단 중앙**: 60초 틱 게이지 바 + `R ▸ COMMIT` 힌트
- **상단 우**: 슬롯 인디케이터 `[I][MY][ME][MINE]` — 채워진 슬롯 점등, 시체 슬롯은 `✕`
- **하단 좌**: `OVERWRITE ●`(남음) / `○`(소진)
- **하단 우**: `DEBT n` (n>0이면 붉게), `ALERTS n`
- **루프 전환 오버레이** (≤ 2초, PRD §15 카피 준수):
  ```
  CAPTURED.
  YOUR ACTIONS WILL RETURN.
  LOOP 2 — YOU ARE "ME"
  ```
- **클리어 오버레이**: `ESCAPED`(마지막 스테이지는 `ALL ESCAPED`) / 시간 / `AFTERIMAGES n / PAR n` /
  `ALERTS n` / par 달성 시 `MINIMUM AFTERIMAGE ★` / 누적 `DEBT n` / `ENTER ▸ NEXT`
- **런 결과 오버레이** (GAUNTLET / TIME ATTACK 만): `RUN COMPLETE` + 스테이지별 스플릿
  (시간 / 잔상 / 알람 / 메달) + 총계 + 이전 최고 기록 대비. §12.
- **타이틀**: `I.MY.ME.MINE` + 태그라인 `Fail now. Escape later.` + **두 줄 선택 UI**
  (위 = 플레이 방식 `[ STORY ] [ GAUNTLET ] [ TIME ATTACK ]`, 아래 = 감지 방식 `EASY` / `LISTEN`) +
  `S ▸ 인트로 다시보기`. 클릭/Enter 즉시 시작, 로그인·긴 메뉴 없음(PRD §8.1).
- **LISTEN HUD**: 좌상단 `LISTEN` 배지 + **4칸 음량 미터**(첫 칸 = 안전 구간 레벨 0, 둘째 칸부터
  붉게 맥동) + 현재 감도. 캘리브레이션 중에는 `조용히 해주세요 — 환경음 측정 중`.
- **속말 한 줄**(하단)과 **목표 한 줄**(상단): 고정 힌트가 아니라 상황이 고르는 대사다. §13.

---

# 9. 스테이지 15종 · 4막 (수제)

`par` = 메달 기준 잔상 수. 사용 잔상 수 ≤ par → **MINIMUM AFTERIMAGE ★**.
레벨은 **순수 데이터**(`levels.ts`). 타일맵은 문자열 배열(`#`벽 `.`바닥 `S`스폰), 장치는 좌표 배열.
좌표 설계는 "걷기 1타일 = 16틱" 위에서 손으로 계산한다(§4).

**막마다 새 규칙이 정확히 하나.** 1막 수용(01~04) 새 장치 없음 / 2막 감시(05~08) 경비·CCTV·레버 +
`grate` / 3막 통제(09~12) `seqButton`·`laser` / 4막 경계(13~15) `powerBus`.

| # | id / 이름 | par | 새 장치 | 설계 발상 |
|---|---|---|---|---|
| 1 | `01_I` **THE CELL** | 1 | 발판 · 게이트 | 발판을 밟은 채로 나를 남기면 그 문은 계속 열려 있다 (**코어 아하**, Time-to-Aha < 30초 목표) |
| 2 | `02_I` **THE CLOCK** | 1 | 시간차 버튼(`holdTicks 90`) | 잔상은 **자리**가 아니라 **순간**을 맡을 수도 있다 (버튼↔게이트 18타일 = 달려도 시간으로 봉쇄) |
| 3 | `03_I` **THE WEIGHT** | 1 | 상자 · 경비 | 발판은 무게만 묻는다. 상자 하나가 한 몸을 대신한다 |
| 4 | `04_I` **THE ANTECHAMBER** | 2 | 잠긴 문 안쪽의 발판 | 두 번째 나는 **첫 번째 나가 연 문으로만** 들어간다 — 잔상에 **순서**가 생긴다 |
| 5 | `05_MY` **THE CORRIDOR** | 2 | 순찰 경비 + 소음 미끼 | 간수는 소리를 쫓는다 — 잔상은 **유인 자원**이다 (기하 + 시간 이중 봉쇄) |
| 6 | `06_MY` **THE GRATE** | 2 | **grate** | **빠른 길은 시끄럽다.** 격자 지름길(≈304틱)과 무음 우회로(≈496틱)가 **둘 다** par 안에서 성립 |
| 7 | `07_MY` **THE SEAL** | 2 | CCTV · 레버 | 감시안을 끄는 손과 발판을 밟는 발이 **같은 몸**일 수 있다 (한 잔상 2역) |
| 8 | `08_MY` **THE VIGIL** | 2 | grate × 레버 | **스위치가 조용히 갈 수 없는 곳에 있다** — 4막의 강제 트레이드오프를 소음으로 미리 배운다 |
| 9 | `09_ME` **THE ORDER** | 2 | **seqButton** | 순서를 밟으려면 **문을 잡아 줄 사람이 먼저** 있어야 한다. 틀리면 0으로 되돌아간다 |
| 10 | `10_ME` **THE BEAM** | 2 | **laser** | 레이저는 완전히 예측 가능하다 — 외우는 게 아니라 **통과할 시각을 고른다** |
| 11 | `11_ME` **THE TIMETABLE** | 3 | 버튼 2개 × seqButton | **잔상들이 시각표를 나눠 갖는다.** 각자 다른 시각에 다른 문을 90틱씩 연다 |
| 12 | `12_ME` **THE THRESHOLD** | 3 | 복합 (3막의 벽) | 상자·레버·버튼·미끼가 **전부 다른 몸**에 걸린다. `넷이 전부다.` |
| 13 | `13_MINE` **THE BREAKER** | 3 | **powerBus** | **문을 열면 감시안이 켜진다.** 전력은 하나뿐이라 둘을 동시에 가질 수 없다 |
| 14 | `14_MINE` **THE JUNCTION** | 3 | powerBus × 3채널 | 위험이 셋인데 전력은 하나다 — **셋을 시간으로 나눠 통과한다** |
| 15 | `15_MINE_FINAL` **THE LAST DOOR** | 3 | 전 장치 총동원 | **코드를 끝내면 길이 열리고, 문을 열면 그 길이 다시 닫힌다.** 먼저 건너가 있어야 한다 |

- **par 는 막을 넘어갈 때 뒤로 가지 않는다**(난이도 곡선 — `tests/solvability.test.ts` 가 검사).
- 모든 스테이지는 **잔상 0개로 클리어 불가**여야 한다. 구조 증명 + 실제 단독 공략 시도를 굴려
  실패를 확인한다(`tests/rules.test.ts`). 혼자 풀리는 스테이지는 코어 메커니즘이 무의미하므로 **블로커**다.
- 3분 데모 루트는 스테이지 선택 없이 **1번부터 순서대로** 진행한다(README 참조).

---

# 10. 완료 기준 (검증하고 증거와 함께 보고할 것)

**아래 4개를 실제로 실행해 통과 출력을 확인하기 전까지 "완료"라고 보고하지 말 것.**

1. `npm run build` — TypeScript `strict: true`, 오류/경고 0 (`tsc --noEmit` 이 빌드에 포함된다).
2. `npm test` (`node --test --import tsx tests/*.test.ts`) — **8개 파일 전부 통과, 실패 0**:
   - **`determinism.test.ts`**: 무작위 입력 테이프를 각각 2번씩 시뮬 → 최종 상태 해시(FNV-1a,
     모든 엔티티 좌표/상태/타이머 포함)가 **완전히 동일**. 불일치 0.
   - **`solvability.test.ts`**: **15개 스테이지 각각**에 하드코딩된 정답 테이프를 헤드리스 시뮬에
     넣어 `cleared === true` **그리고** 사용 잔상 수 `<= par` 확인. 6번은 두 경로 모두 검증.
     → 통과하지 않는 스테이지는 **미완성이다. 레벨 좌표를 고쳐서 통과시킬 것.**
   - **`rules.test.ts`**: SPEC 규칙 대조군 검증 + **15개 스테이지 전부 잔상 0개 클리어 불가** 증명.
   - **`devices.test.ts`**: grate/laser/seqButton/powerBus 규칙, 4장치 합성 레벨의 결정론,
     그리고 **장치를 쓰지 않는 레벨의 무영향 보증**.
   - **`movement.test.ts`**: 코너 어시스트가 "1타일 = 16틱"과 잔상 재생 일치를 깨지 않음.
   - **`modes.test.ts`**: `PlayMode` 가 시뮬 해시를 바꾸지 않음, DEBT/시간 누적, 두 축 독립,
     `localStorage` 차단 환경 내구성.
   - **`mic.test.ts`**: 마이크 비트가 섞여도 재생 오차 0, 레벨별 소음 발생/미발생(대조군).
   - **`whisper.test.ts`**: 속말이 `SimState`/`Session` 을 오염시키지 않음(해시 동일).
   - **측정치는 추정하지 말고 실행 출력을 쓸 것.** 현재: **159 tests / 32 suites / fail 0.**
3. 브라우저 실기동 확인: 인트로 → 타이틀(두 축 선택) → 스테이지1 이동 → `R` 확정 → 잔상 재생 →
   게이트 통과 → loot → escape → 클리어 오버레이.
4. `README.md`: **플레이 링크(맨 위)** / 컨셉 / 15스테이지 표 / 장치 / 두 축 / LISTEN(+개인정보) /
   조작 / 결정론 아키텍처 / 테스트 실행법 / CI·배포 / AI 활용 / 심사용 3분 데모 루트.
5. CI(`.github/workflows/deploy.yml`): `tsc --noEmit` + 전체 테스트를 통과해야만 Pages 에 배포되고,
   프로덕션 번들에 개발용 훅(`__imm`)이 유출되면 **배포를 실패시킨다**.
6. **검증되지 않은 주장을 문서에 쓰지 않는다.** 사람 플레이테스트를 하지 않았다면 "재미있다"를
   증명된 것처럼 쓰지 말 것 — 기계로 증명한 것은 "풀린다 / 혼자서는 안 풀린다"까지다.

---

# 11. 파일 구조

```
imymemine/
  index.html
  package.json         vite, typescript, tsx(테스트용)만. 런타임 의존성 0.
  tsconfig.json        strict: true, noUncheckedIndexedAccess: true
  vite.config.ts       base: './'
  README.md
  SPEC.md              (이 문서)
  src/
    main.ts            부트스트랩, 페이즈 전환(INTRO / TITLE / PLAY / TRANSITION / CLEAR / ALLCLEAR)
    engine/
      loop.ts          accumulator 고정 틱
      input.ts         키보드 → InputMask + 메타키 엣지 큐 (녹화 대상과 분리)
      audio.ts         WebAudio 절차 생성 SFX
      mic.ts           LISTEN 모드 마이크 → 0~3 레벨 (시뮬 밖. 저장·전송 없음)
    sim/
      constants.ts     SUBPIXEL/TILE/속도/틱 상수 전부 여기 (매직넘버 금지)
      types.ts         SimState, Body, Crate, Guard, Device, Laser, SeqGroup, PowerBus, LevelDef
      world.ts         createWorld(level, ghosts) / stepWorld(state) — 순수 함수, DOM 무접근
      physics.ts       AABB, 축분리 충돌, 상자 밀기, 코너 어시스트, 정수 레이캐스트
      guard.ts         경비 상태기계
      devices.ts       채널 평가, 발판/버튼/레버/게이트/CCTV + grate/seqButton/laser/powerBus
      hash.ts          상태 해시 (FNV-1a, 결정론 테스트용)
    game/
      session.ts       루프 진행, commit/overwrite/reset, DEBT, 메달, 알람,
                       PlayMode(STORY/GAUNTLET/TIME_ATTACK) · 런 기록(localStorage)
      levels.ts        15개 스테이지 4막 데이터
      whisper.ts       상황 트리거 속말 + 단계 힌트 (읽기 전용, DOM 무접근)
    render/
      palette.ts       팔레트의 단일 진실
      renderer.ts      월드 드로우 (트레일/글로우/셰이크/시설 레이어) + ViewState
      figure.ts        얼굴 없는 실루엣 인물 · 잉크 스트로크
      intro.ts         7칸 3페이지 만화 인트로 (자체 틱, 시뮬 무접근)
      hud.ts           HUD + 오버레이 + 타이틀 + 런 결과
  tests/
    determinism.test.ts  devices.test.ts  mic.test.ts          modes.test.ts
    movement.test.ts     rules.test.ts    solvability.test.ts  whisper.test.ts
  .github/workflows/deploy.yml   verify(tsc+test) → build(+`__imm` 유출 검사) → Pages
```

`engine/tape.ts` 는 만들지 않았다 — 테이프는 `Uint8Array` 하나와 `session.ts` 의
`recording`/`commitLoop` 로 충분해서 별 모듈이 필요하지 않았다.

`sim/` 은 **DOM·브라우저 API에 절대 접근하지 않는다.** 그래야 헤드리스 테스트가 가능하다.

---

# 12. 두 개의 직교 축 — 플레이 방식 × 감지 방식

### 12.1 플레이 방식 (`PlayMode` — `game/session.ts`)

| 값 | 성적 기준 | 동작 |
|---|---|---|
| `STORY` | 없음 (기록 미저장) | 스테이지를 하나씩 클리어하며 나아간다 |
| `GAUNTLET` | **누적 DEBT**, 동률이면 잔상 수 | 1번부터 마지막까지 이어서 한 런 |
| `TIME_ATTACK` | **누적 경과 틱** | 같이 이어서 하되 총 시간이 성적 |

- **`PlayMode` 는 세션/표시 레이어 전용이다.** `SimState` 에 닿지 않고 `stepWorld` 의 동작을 한 틱도
  바꾸지 않는다 (`tests/modes.test.ts` 가 상태 해시로 증명).
- **타이틀에서만 바꿀 수 있다** — 런 도중에 바뀌면 기록의 기준이 흔들린다.
- 부채가 리셋되지 않는 범위는 **런 하나**이고, 그 경계는 `startRun` 이다. `startStage`/`fullReset`
  은 DEBT 를 지우지 않는다(`fullReset` 은 오히려 +1).
- 스테이지 스플릿은 클리어 시 **딱 한 번** 쌓인다(시간 / 잔상 / 알람 / 메달).
- 기록은 `localStorage` 에 `imm.best.{playMode}.{mode}` 로 저장한다 — **두 축을 모두 키에 넣는다**
  (LISTEN 런과 EASY 런은 난이도가 달라 같은 표에 올릴 수 없다).
  접근 자체가 예외를 던지는 환경(사파리 프라이빗 등)에서도 **런은 정상 진행되고 저장만 실패한다.**
  네트워크로는 아무것도 보내지 않는다.

### 12.2 감지 방식 (`Mode`)

`EASY` / `LISTEN` — §4.1. 스테이지 진행이나 초기화로 바뀌지 않는 **세션 속성**이다
(`startStage`/`fullReset` 은 이 필드를 건드리지 않는다). 두 축은 자유롭게 조합된다.

---

# 13. 속말 (whisper) — 힌트를 설명서에서 독백으로

HUD 하단의 고정 힌트 한 줄은 설명서였다. `game/whisper.ts` 가 그 자리를 주인공의 생각으로 바꾼다.

- **상황 트리거 기반.** 타이머 순서가 아니라 **상태**가 대사를 고른다 — 닫힌 게이트에 부딪힘,
  발판 위 2초/3초 체류, 잔상이 발판 위에서 정지, 간수 의심/추격, 잔여 10초, 코어 획득, 슬롯 소진.
  급한 대사(추격·체포·시간)는 떠 있는 대사를 밀어내고 끼어들고, 자리가 없는 대사는 최대 2개까지
  잠깐 물고 있다가 시한(480프레임)이 지나면 버린다.
- **막히면 단계적으로 구체화.** 진전 서명(잔상 수 · 코어 소유 · 켜진 채널 집합 · 조작 몸의 타일 좌표)이
  `STALL_FRAMES = 720`(≈12초) 동안 그대로면 다음 단계 힌트를 준다. **첫 단계가 정답이면 퍼즐이 죽으므로
  힌트는 뒤로 갈수록만 구체적이어야 한다.** 15개 스테이지 전부 오프닝 + 최소 2단계를 갖는다.
- **읽기 전용 · DOM 무접근.** `SimState`/`Session` 의 어떤 필드도 쓰지 않고, 그리는 일은 `render/hud.ts`
  가 한다. 타이머는 **프레임 기준**이라 시뮬 틱이 멈추는 TRANSITION 에서도 대사가 굳지 않는다.
  `tests/whisper.test.ts` 가 "속말을 끼워 돌린 런과 안 끼운 런의 최종 해시가 같다"를 증명한다.

---

# 14. 인트로 만화 (7칸 · 3페이지)

- 전면 슬라이드쇼가 아니라 **만화 페이지**다. 칸이 하나씩 잉크가 번지듯 그려지고 연출이 끝나면
  입력이 올 때까지 멈춘다. 다음 키/탭에서 다음 칸을 열고, 페이지 경계에서만 가로로 넘어간다.
  칸당 연출은 최대 132틱(2.2초). 페이지1 = 칸 1~3, 페이지2 = 칸 4~6, 페이지3 = 칸 7(전면).
- 칸마다 샷 사이즈가 다르고(와이드 / 미디엄 / 익스트림 클로즈업), 명암은 **하프톤 스크린톤**으로 만든다.
- **회수 구조**: 칸 2의 "넷씩 묶인 금"이 칸 7에서 **네 인물** 뒤로 다시 떠오른다. 두 칸은 같은 시드
  (`TALLY_SEED`)로 같은 획을 그려야 회수로 읽힌다.
- 인물은 **얼굴 없는 실루엣**이다. 감정은 이목구비가 아니라 자세·빛·구도로만 만든다(`figure.ts`).
- `sim/` 과 완전히 분리된 순수 렌더 + 자체 틱 카운터. SimState 를 만들지도 읽지도 않는다.
- 최초 1회만 자동으로 열고(`localStorage`, 접근 실패 시 "본 적 없음"으로 처리), 이후 타이틀에서 `S`.
  재생 중에는 **아무 키나** 다음 칸이며, 마지막 칸을 끝내는 데 쓴 `Enter` 가 곧바로 스테이지를
  시작시키지 않도록 빠져나갈 때 입력 엣지를 비운다.

---

# 15. 금지 사항

- 미구현 스텁, `TODO` 주석, `test.skip`/`test.only`, placeholder 레벨 — **전부 블로커**.
- 시뮬 내부의 부동소수점 / `Math.random()` / `Date.now()` / DOM 접근.
- 외부 런타임 의존성 추가(빌드·테스트 도구 제외).
- 외부 이미지·오디오 에셋 파일.
- 런타임 네트워크 호출 일체.
