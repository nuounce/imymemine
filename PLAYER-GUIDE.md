# I.MY.ME.MINE 플레이어 공략집

15개 방을 전부 par 이내로, 경보 0회, DEBT 0으로 통과한 기록입니다.
각 방마다 **몸을 몇 개 쓰고 누가 무엇을 맡는가**만 정리했어요.

| STAGES | MEDALS | AFTERIMAGES | ALERTS | DEBT | TIME |
| ------ | ------ | ----------- | ------ | ---- | ---- |
| 15 / 15 | 15 | 32 | 0 | 0 | 07:09.45 |

엔딩은 `NOTHING LEFT BEHIND`(넷이 함께 나가는 쪽)입니다. DEBT가 1 이상이면 엔딩이 갈리니
`BACKSPACE` 초기화는 정말 마지막 수단으로만 쓰세요.

> 웹 버전 공략집(스크린샷 포함): <https://claude.ai/code/artifact/c6fcb0ab-1b62-4fce-beb2-0f705a343ea2>
>
> 규칙의 정밀 정의는 [`SPEC.md`](./SPEC.md), 스테이지 실측 QA는 [`WALKTHROUGH.md`](./WALKTHROUGH.md)를 보세요.
> 이 문서는 **플레이어용**입니다.

---

## 1. 먼저 알아야 할 여섯 가지

이걸 모르면 손이 아무리 좋아도 안 풀립니다. 특히 두 번째와 네 번째가 그래요.

**잔상은 영상이 아니라 입력이다.**
저장되는 건 궤적이 아니라 틱마다 누른 키입니다. 루프가 넘어가면 세계가 0틱부터 통째로 다시
돌아요. 그래서 과거의 내가 경비를 끌고 가면 그 경비는 이번 판에서도 진짜로 그쪽에 가 있습니다.

**한 타일 = 16틱.**
걷기는 초당 3.75타일입니다. 한 칸이 정확히 16틱이고 네 칸이 딱 1초를 조금 넘어요.
대기 시간을 "몇 칸 걸을 시간"으로 환산하면 손으로도 셀 수 있습니다.

**걷기는 무음, 달리기는 소음.**
`SHIFT`를 놓고 걸으면 아무 소리도 안 납니다. 달리면 12틱마다 반경 5타일로 소리가 퍼져요.
격자 바닥 위에서는 **걸어도** 10틱마다 4타일씩 웁니다.

**덩치는 좁은 길에 못 들어온다.**
BRUTE는 몸이 40px이라 32px짜리 1타일 통로에 물리적으로 안 들어갑니다. AI 예외가 아니라
충돌 판정의 결과예요. **문칸 한 칸이 곧 피난처**입니다.

**발판과 버튼은 다르다.**
발판(plate)은 올라가 있는 동안 켜집니다. 몸이든 시체든 상자든 무게만 있으면 돼요.
버튼(button)은 누른 뒤 정해진 틱 동안만 켜졌다 꺼집니다.

**R은 실패가 아니라 배치다.**
`R`로 지금의 나를 그 자리에 확정합니다. 발판 위에서 R을 누르면 그 문은 남은 판 내내
열려 있어요. 60초를 다 쓸 이유가 전혀 없습니다.

### 조작

| 키 | 하는 일 |
| --- | --- |
| `WASD` / 방향키 | 이동 |
| `SHIFT` | 달리기. 발소리가 나고 경비가 듣습니다 |
| `E` | 버튼, 레버 상호작용 (사거리 40px) |
| `F` | 눈뽕. 주웠을 때만 작동하고 1회용입니다 |
| `R` | 루프 조기 확정. 지금까지의 나를 잔상으로 남깁니다 |
| `Q` → `1/2/3` | 잔상 하나를 지우고 다시 녹화. 스테이지당 1회 |
| `BACKSPACE` 2초 | 스테이지 전체 초기화. DEBT +1, 절대 안 지워집니다 |

---

## 2. 경비 네 유형

각자 다른 것으로 위협합니다. 무엇이 무서운지 알면 무엇을 안 해도 되는지가 보여요.

| 유형 | 크기 | 시야 | 순찰 | 추격 | 무엇이 무서운가 |
| --- | --- | --- | --- | --- | --- |
| SENTRY | 26px | 7타일 / 50° | 보통 | 빠름 | 기본형입니다. 대기 중 좌우로 45°씩 훑으니 "정면만 피하면 된다"가 통하지 않아요 |
| HOUND | 24px | 5.5타일 / 35° | 빠름 | 가장 빠름 | 눈이 아니라 **냄새**로 320틱 전 궤적까지 되짚습니다. 한 번 물면 300틱을 안 놓아요. 잔상이 지나간 길도 미끼가 됩니다 |
| BRUTE | 40px | 6타일 / 65° | 느림 | 느림 | 시야각이 넓고 발각이 빠릅니다. 대신 느리고 1타일 통로에 못 들어와요. 넓은 방에서만 무섭습니다 |
| WATCHER | 28px | 10타일 / 40° | 고정 | 없음 | 직접 잡지 않습니다. 대신 게이지가 차면 **경보**를 울려 반경 7타일 안의 경비를 전부 불러요 |

---

## 3. 1막 수용 (01 ~ 04)

새 장치 없음. 발판, 게이트, 상자, 그리고 조기 확정을 배웁니다.

### 01. THE CELL (par 1, 잔상 1, 275틱)

문은 발판을 밟고 있는 동안만 열립니다. 그러니 발판 위에서 나를 남기면 돼요.

- **MY**: 왼쪽 1칸 → 아래 3칸으로 발판 `(3,7)` 위에 서고 그대로 `R`. 문이 계속 열립니다.
- **I**: 아래 1칸 → 오른쪽 11칸 → 위 2칸에서 코어 → 아래 4칸으로 탈출.

이 방의 전부는 "R은 죽는 게 아니라 배치하는 것" 하나입니다. 여기서 이걸 못 잡으면 뒤가 전부 막혀요.

### 02. THE CLOCK (par 1, 잔상 1, 428틱)

버튼은 90틱(1.5초)만 문을 엽니다. 버튼과 문은 18타일 떨어져 있어서 달려도 못 닿아요.
잔상이 맡는 건 자리가 아니라 **순간**입니다.

- **MY**: 왼쪽 3칸 → 아래 5칸으로 버튼 `(1,7)`. 도착해도 바로 누르지 말고 기다렸다가
  약 4.7초(280틱)에 `E` → `R`.
- **I**: 아래 2칸 → 오른쪽 14칸으로 문 바로 앞 `(18,4)`에 미리 가서 대기.
  잔상이 누르는 그 순간 오른쪽 4칸 → 위 2칸 코어 → 아래 4칸 탈출.

순서가 반대입니다. 내가 먼저 문 앞에 가 있고 버튼은 나중에 잔상이 눌러요.
잔상이 몇 초에 누르는지 세어 두세요. 그게 곧 내 출발 신호입니다.

### 03. THE WEIGHT (par 1, 잔상 1, 437틱)

발판은 무게만 묻습니다. 사람이 아니어도 돼요. 상자 하나가 한 몸을 대신합니다.

- **MY**: 왼쪽 1칸 → 상자를 아래로 밀어 발판 A `(2,7)`에 넣고 → 오른쪽 3칸 →
  발판 B `(5,7)` 위에서 `R`. 한 몸이 발판 두 개를 채웁니다.
- **I**: 아래 3칸 → 오른쪽 15칸 → 위 3칸 코어 → 아래 5칸 탈출.

1막에는 경비가 없습니다. 상자 미는 감각만 익히세요. 상자는 몸이 닿은 방향으로 밀립니다.

### 04. THE ANTECHAMBER (par 2, 잔상 2, 775틱)

두 번째 잔상은 **첫 번째 잔상이 연 문으로만** 들어갑니다. 여기서 잔상에 순서가 생겨요.

- **MY**: 왼쪽 3칸 → 아래 4칸으로 바깥 발판 `(1,7)` → `R`. 바깥 문 `(9,5)`이 열립니다.
- **ME**: 열린 바깥 문으로 들어가 아래 2칸 → 오른쪽 8칸 → 아래 2칸으로 안쪽 발판 `(12,7)` → `R`.
- **I**: 아래 2칸 → 오른쪽 18칸 → 위 3칸 코어 → 아래 5칸 탈출.

순서를 바꾸면 안 됩니다. 안쪽 발판을 먼저 노리면 그 몸이 바깥 문 앞에서 막혀서
방에 들어가지도 못해요. 잔상은 남긴 순서대로 재생됩니다.

---

## 4. 2막 감시 (05 ~ 08)

경비, CCTV, 레버, 그리고 소음 바닥(격자). 소리가 자원이 됩니다.

### 05. THE CORRIDOR (par 2, 잔상 2, 823틱)

금고문은 발판 `(22,8)`과 버튼 `(2,1)`이 둘 다 켜져야 열립니다. 정반대 구석이에요.
게다가 경비가 금고로 가는 유일한 복도를 순찰합니다.

- **MY**: 위 3칸 → 오른쪽 8칸 → 아래 3칸으로 남쪽 방에 들어가서, `SHIFT`를 잡고
  좌우로 약 2.7초 뛰어 소음을 냅니다. 그다음 오른쪽 10칸 → 발판 `(22,8)`에서 `R`.
- **ME**: 왼쪽 2칸 → 위 7칸으로 버튼 `(2,1)`. 기다렸다가 약 10.5초(632틱)에 `E` → `R`.
- **I**: 약 6.1초 제자리 대기 → 위 3칸 → 오른쪽 15칸으로 복도 종주 → 위 3칸 →
  왼쪽 2칸 코어 `(17,2)` → 오른쪽 5칸 → 위 1칸 탈출 `(22,1)`.

한 잔상이 미끼와 무게추 2역입니다. 뛰어서 경비를 남쪽으로 끌어낸 그 몸이 그대로
발판에 눌러앉아요. 뛰는 건 소음 낼 때만, 나머지는 전부 걷습니다.

### 06. THE GRATE (par 2, 잔상 2, 1239틱)

지름길은 격자 바닥을 지나갑니다. 격자 위에서는 걸어도 소리가 나요.
남쪽 우회로는 완전 무음이지만 12타일이 더 멉니다. 그 3.2초를 누가 치를지가 이 방의 전부예요.

- **MY**: 아래 8칸 → 왼쪽 3칸으로 발판 `(1,10)` → `R`. 남쪽 무음 경로로 갑니다.
- **ME**: 약 10.5초 대기 → 아래 3칸 → 오른쪽 7칸(여기서 격자를 밟고 웁니다) →
  아래 2칸 → 버튼 `(11,7)` `E` → `R`. 버튼은 240틱(4초) 유지됩니다.
- **I**: 약 8.6초 대기 → 아래 8칸 → 오른쪽 16칸으로 **남쪽 무음 우회로** → 위 5칸 →
  오른쪽 6칸 → 위 3칸 코어 → 아래 8칸 탈출.

HOUND가 `(12,2)`에서 `(12,8)`을 오르내립니다. 남북 끝에 있을 때는 격자에서 4.2타일이라
소음 반경 밖이에요. 소음은 잔상에게 떠넘기고 나는 조용한 긴 길로 갑니다.

### 07. THE SEAL (par 2, 잔상 2, 759틱)

감시안을 끄는 손과 발판을 밟는 발이 **같은 몸**일 수 있습니다.
레버를 내리고 멈추지 말고 발판까지 계속 가세요.

- **MY**: 아래 4칸 → 레버 `(2,7)` 사거리 안에서 `E` → 그대로 오른쪽 3칸 →
  발판 `(6,7)` 위에서 `R`. 잔상 하나로 두 가지가 됩니다.
- **ME**: 위 2칸 → 버튼 `(4,1)`. 약 2.2초(131틱)에 `E` → `R`. 버튼은 180틱 유지.
- **I**: 약 1.7초 대기 → 아래 2칸 → 오른쪽 19칸 → 위 4칸으로 코어 `(22,1)` →
  아래 7칸 → 왼쪽 11칸 탈출 `(11,8)`.

BRUTE 첫 등장입니다. 금고 위쪽 띠만 3타일 넓이고 나머지는 전부 1타일이라
덩치는 위 띠에 갇혀 있어요. 코어를 낚아채면 뒤돌아보지 말고 세로 통로로 떨어지세요.
못 따라 내려옵니다.

---

## 5. 08. THE VIGIL 집중 공략 (par 2, 잔상 2, 853틱)

스위치가 **조용히 갈 수 없는 곳**에 있습니다. 레버 `(14,6)`는 격자판 `(12~16, 5~7)`
한복판이라 어느 몸이 가든 반드시 울어요. 그 발소리가 곧 덩치를 부릅니다.

### 왜 막히는가

방은 셋입니다. 왼쪽 시작방, 가운데 격자방, 오른쪽 금고.
왼쪽과 가운데를 잇는 문칸 `(10,6)`과 가운데와 금고를 잇는 문칸 `(20,6)`이
**y=6 한 줄에 있는 유일한 통로**예요. 위아래로 도는 길은 아예 없습니다.

1. 금고문 `(20,6)`은 발판 `(1,1)`이 눌려 있어야 열립니다. 그 발판은 지도 정반대 구석이라
   한 몸으로는 절대 안 됩니다.
2. 금고문 앞에 CCTV `(19,6)`가 있고, 그걸 끄는 레버는 격자 한복판입니다.
   레버로 가는 길이 곧 소음이에요.
3. BRUTE가 격자방 `(16,2)`에서 `(16,10)`을 세로로 오갑니다. 그 순찰선이 y=6을
   가로질러요. 즉 내가 건너야 하는 딱 그 줄입니다.
4. 금고 안에서는 SENTRY가 `(23,3)`에서 `(23,9)`를 훑습니다.
   코어 `(24,2)`와 탈출구 `(24,10)`는 그 줄 바로 옆이고요.

### 배치

- **MY**: 왼쪽 2칸 → 위 1칸으로 발판 `(1,1)`. 바로 `R`. 1초도 안 걸립니다.
  이 몸은 금고문을 남은 판 내내 열어 두는 일만 해요.
- **ME**: 아래 4칸 → 오른쪽 11칸으로 레버 `(14,6)`까지 직진, `E` 누르고 그 자리에서 `R`.
  감시안이 꺼지고, 그 발소리가 덩치를 남쪽으로 끌고 갑니다. 스위치이면서 미끼예요.
- **I**: 시작하자마자 움직이지 **말고** 약 4.6초(274틱) 기다립니다. 그다음 아래 4칸 →
  오른쪽 21칸으로 한 번도 멈추지 않고 종주 → 위 4칸 코어 `(24,2)` → 아래 8칸 탈출 `(24,10)`.

### 손으로 칠 수 있는 창

출발 시각을 2틱 간격으로 전부 돌려서 성공 구간을 실측했습니다.

| 출발 틱 | 초 환산 | 폭 | 비고 |
| --- | --- | --- | --- |
| 16 ~ 22 | 0.27 ~ 0.37초 | 7틱 | 덩치 바로 뒤를 따라붙는 창. 사실상 못 노립니다 |
| 248 ~ 258 | 4.13 ~ 4.30초 | 11틱 | |
| **262 ~ 296** | **4.37 ~ 4.93초** | **35틱** | **가장 넓은 창. 여기를 노리세요** |
| 300 / 304 | 5.00 / 5.07초 | 1틱씩 | |
| 308 ~ 314 | 5.13 ~ 5.23초 | 7틱 | |

- 노릴 곳은 **262~314틱**, 대략 **4.4초에서 5.2초 사이**입니다.
  그중 262~296은 35틱이 통째로 이어져 있어요. 약 **0.6초**의 여유입니다.
- 기다리는 위치는 상관없습니다. 제자리에서 기다리든 서쪽 문칸 `(11,6)`까지 가서 기다리든
  성공 구간이 똑같이 나왔어요. 중요한 건 **격자방에 발을 들이는 시각** 하나뿐입니다.
- 레버 몸(ME)의 출발은 훨씬 관대합니다. 0~164틱(약 2.7초) 늦게 출발해도 전부
  클리어됐어요. 여기서 스트레스 받지 마세요.

눈으로 잡을 신호는 하나입니다. **덩치가 격자방 남쪽 끝 바닥까지 내려간 것을 보고 출발하세요.**

### 어디서 잡혔는지가 곧 진단표다

| 잡힌 좌표 | 원인 | 고칠 것 |
| --- | --- | --- |
| `(14,6)` ~ `(16,6)` | 격자방 한복판에서 BRUTE에게 잡혔습니다. 가장 흔한 실패예요 | 출발이 너무 이르거나 너무 늦었습니다. 덩치가 방 남쪽 끝에 닿은 걸 눈으로 보고 출발하세요 |
| `(24,3)` ~ `(24,6)` | 금고 안에서 SENTRY에게 잡혔습니다. 격자방은 통과했다는 뜻이에요 | 출발이 조금 어긋나 금고 도착 시각이 밀렸습니다. 격자방을 절대 멈추지 말고 한 번에 건너세요 |
| 문 앞에서 못 들어감 | 금고문이 안 열렸습니다 | 첫 잔상이 발판 `(1,1)` 위에 정확히 올라가 있는지 확인하세요. 옆 칸이면 안 켜집니다 |
| CCTV에 걸림 | 레버가 안 내려갔습니다 | 두 번째 잔상이 `E`를 실제로 눌렀는지 보세요. 레버 사거리는 40px, 한 칸 조금 넘습니다 |

코어를 집은 뒤에는 뒤를 신경 쓰지 마세요. 덩치가 뒤늦게 문칸까지 따라오지만
40px 몸으로는 그 한 칸에 들어오지 못합니다. 오른쪽 끝 줄을 타고 그대로 아래로 내려가면 끝이에요.

### 그래도 안 되면

1. 1번, 2번 잔상만 먼저 완벽하게 만드세요. 둘 다 타이밍이 거의 자유롭습니다.
   발판 몸은 1초, 레버 몸은 2.7초까지 늦어도 됩니다.
2. 3루프에서는 일부러 잡혀 보세요. 잡힌 좌표가 위 표의 어느 줄인지 보면
   이르게 갔는지 늦게 갔는지가 바로 나옵니다.
3. 세 번째 잔상까지 써도 됩니다. 메달은 못 받지만 클리어는 클리어예요. 몸은 넷까지 있습니다.
4. `Q` → `1/2/3`으로 잔상 하나만 다시 녹화할 수 있습니다. 스테이지당 한 번뿐이니
   3번 몸에 아껴 쓰세요.
5. `BACKSPACE` 2초 홀드는 마지막 수단입니다. DEBT가 영구히 남고 엔딩이 갈려요.

---

## 6. 3막 통제 (09 ~ 12)

순차 버튼, 주기 레이저, 눈뽕. 여기서 네 번째 몸까지 씁니다.

### 09. THE ORDER (par 2, 잔상 2, 1025틱)

벽장 셋의 버튼을 0 → 1 → 2 순서로 밟아야 합니다. 그런데 벽장 B와 C의 문은
각각 지도 양 끝의 발판이 잡아 줘야 열려요. 한 몸으로는 순서를 끝까지 밟을 수가 없습니다.

- **MY**: 아래 5칸 → 서쪽 발판 `(2,11)` → `R`.
- **ME**: 아래 5칸 → 오른쪽 19칸으로 바닥 길을 종주해 동쪽 발판 `(21,11)`.
  가는 길 `(14,11)`에서 눈뽕을 자동으로 줍습니다. 발판 위에서 약 10.3초(620틱)에
  `F`로 터뜨리고 `R`.
- **I**: 오른쪽 3칸 → 위 3칸 버튼0 `E` → 내려와 대기 → 오른쪽 7칸 → 위 2칸 버튼1 `E` →
  오른쪽 7칸 → 위 2칸 버튼2 `E` → 아래 6칸 → 오른쪽 7칸 → 코어 `(26,7)` → 아래 4칸 탈출.

동쪽 발판은 순찰선에서 6.1타일, 즉 시야 안이라 그 몸은 언젠가 반드시 들킵니다.
그게 설계예요. 들키는 그 순간에 눈뽕을 터뜨리면 순찰조와 문지기가 한꺼번에 150틱 눈이 멉니다.
순서를 틀리면 그룹이 0으로 되돌아가고 되감기는 없습니다.

### 10. THE BEAM (par 2, 잔상 2, 587틱)

레이저는 완전히 예측 가능합니다. 주기 140틱 중 50틱만 켜져요.
외우는 게 아니라 통과할 시각을 고르는 겁니다.

- **MY**: 왼쪽 1칸 → 아래 4칸으로 발판 `(2,8)` → `R`.
- **ME**: 아래 1칸 → 오른쪽 14칸 → 위 3칸(가는 길 `(17,3)`에서 눈뽕 획득) →
  버튼 `(17,2)`에 서자마자 `F`로 눈뽕. 바로 아래 `(17,5)`의 문지기가 150틱 감깁니다.
  그다음 약 6.5초에 `E` → `R`.
- **I**: 아래 1칸 → 오른쪽 16칸 → 문 앞에서 대기 → 오른쪽 4칸 → 위 3칸 코어 `(23,2)` →
  아래 6칸 탈출 `(23,8)`.

눈뽕은 내가 주울 수 없는 자리에 놓여 있습니다. 버튼 가는 길 위에만 있어요.
만능 탈출 버튼이 아니라 잔상의 도구로 설계된 겁니다.

### 11. THE TIMETABLE (par 3, 잔상 3, 1289틱)

잔상들이 시각표를 나눠 갖습니다. 각자 다른 시각에 다른 문을 90틱씩 열어 줘요.
여기서 처음으로 몸 넷을 다 씁니다.

- **MY**: 아래 5칸 → 오른쪽 22칸으로 바닥 길 종주(`(18,11)`에서 눈뽕 획득) →
  버튼 `(24,11)`을 약 7.5초에 `E`. 그대로 서 있다가 약 17초에 `F` → `R`.
- **ME**: 위 1칸 → 버튼 `(2,5)` 앞. 약 11.6초(696틱)에 `E` → `R`.
- **MINE**: 아래 5칸 → 왼쪽 1칸으로 발판 `(1,11)` → `R`.
  이 발판이 탈출 게이트의 나머지 절반입니다.
- **I**: 순차 버튼 0 `(10,6)` → 1 `(6,3)` → 2 `(15,3)`을 순서대로.
  벽장 문은 잔상이 열어 주는 90틱 안에 들어갔다 나와야 합니다.
  그다음 금고문 `(26,10)` → 코어 `(29,8)` → 탈출 `(29,11)`.

벽장은 한 칸만 깊습니다. 진입 16 + 상승 16 + 누름 2 + 하강 16 + 이탈 16 = 66틱이라
90틱 안에 딱 들어와요. 문이 열리는 시각에 이미 문 앞에 서 있어야 합니다.

### 12. THE THRESHOLD (par 3, 잔상 3, 795틱)

3막의 벽입니다. 상자, 레버, 버튼, 미끼가 전부 다른 몸에 걸려요.
쓸 수 있는 몸은 넷뿐이라 상자로 하나를 아껴야 합니다.

- **MY**: 상자 `(5,2)`를 왼쪽으로 밀어 발판 `(1,2)`에 넣고, 자기는 내려와
  발판 `(1,8)` 위에서 `R`. 한 몸이 발판 두 개를 감당합니다.
- **ME**: 아래 2칸 → 오른쪽 9칸 레버 `(12,5)` `E` → 오른쪽 4칸 버튼 `(16,5)`.
  약 11.7초(700틱)에 `E` → `R`. 버튼은 150틱 유지.
- **MINE**: 남쪽 방으로 내려가 `SHIFT`를 잡고 계속 좌우로 뜁니다.
  HOUND는 한번 물면 300틱을 안 놓기 때문에 그게 그대로 값이 돼요.
- **I**: 약 3.3초 대기 → 아래 2칸 → 오른쪽 23칸 복도 종주 → 아래 3칸 →
  왼쪽 2칸 코어 `(24,8)` → 오른쪽 2칸 → 위 6칸 탈출 `(25,2)`.

북쪽 방은 BRUTE, 남쪽 방은 HOUND입니다. 두 방을 잇는 길이 1타일짜리라
덩치는 두 방 사이를 못 오가요. 탈출구가 있는 북쪽 방이 그의 영역입니다.

---

## 7. 4막 경계 (13 ~ 15)

전력 채널이 등장합니다. 하나를 켜면 하나가 꺼져요.
규칙은 단순합니다. **나중에 새로 켠 쪽이 전력을 가져갑니다.**

### 13. THE BREAKER (par 3, 잔상 3, 800틱)

문을 열면 감시안이 켜집니다. 전력이 하나뿐이라 둘을 동시에 가질 수 없어요.

- **MY**: 아래 5칸 → 오른쪽 5칸 레버 `(8,8)` `E`로 눈을 감기고 →
  왼쪽 7칸 발판 `(1,8)` → `R`.
- **ME**: 왼쪽 2칸 → 위 2칸 발판 `(1,1)` → `R`.
- **MINE**: 약 7.6초(456틱) 대기 → 왼쪽 2칸 → 아래 2칸 발판 `(1,5)` → `R`.
  이 늦은 대기가 문이 열리는 시각을 정합니다.
- **I**: 약 3.7초 대기 → 아래 2칸 → 오른쪽 16칸 → 잠깐 대기 → 오른쪽 5칸 →
  잠깐 대기 → 위 3칸 코어 `(24,2)` → 아래 6칸 탈출 `(24,8)`.

순서가 이 방의 답입니다. 레버로 눈을 먼저 감기고 복도를 건넌 뒤,
잔상 셋이 발판 셋을 동시에 채우는 그 틱에 전력이 문으로 넘어갑니다.
눈은 다시 뜨지만 나는 이미 시야 밖이에요.

### 14. THE JUNCTION (par 3, 잔상 3, 1115틱)

위험이 셋인데 전력은 하나입니다. 레이저, 감시등, 문.
셋을 시간으로 나눠서 통과합니다.

- **MY**: 위 2칸 → 오른쪽 4칸 레버 `(7,1)` `E`(레이저 차단) →
  왼쪽 6칸 발판 `(1,1)` → `R`.
- **ME**: 아래 5칸 → 오른쪽 4칸 레버 `(7,8)`. 약 6.3초(380틱)에 `E`(감시등 차단) →
  왼쪽 6칸 발판 `(1,8)` → `R`.
- **MINE**: 약 9.9초(596틱) 대기 → 왼쪽 2칸 → 아래 2칸 발판 `(1,5)` → `R`.
  이때 문이 열리고 앞의 둘은 꺼집니다.
- **I**: 약 2.5초 대기 → 아래 2칸 → 오른쪽 22칸(분기실 통과) → 대기 →
  문칸 `(26,5)` → 대기 → 오른쪽 1칸 → 위 3칸 코어 `(27,2)` → 아래 3칸 →
  **문칸으로 한 칸 후퇴** → 다시 오른쪽 → 아래 3칸 탈출 `(27,8)`.

가운데 분기실(x17~21)의 출입구는 1타일 두 칸뿐입니다. HOUND가 그 안을 세로로 오가니
개가 남북 끝에 있을 때만 건너세요. 금고의 덩치는 안쪽 끝에 묶여 있어서,
찌르고 곧장 문칸으로 물러나면 못 따라옵니다. 후퇴 한 칸이 이 방의 핵심 동작이에요.

### 15. THE LAST DOOR (par 3, 잔상 3, 1791틱)

코드를 끝내면 길이 열리고, 문을 열면 그 길이 다시 닫힙니다.
순차 버튼 셋을 밟으면 레이저와 감시안이 함께 죽는데, 그게 탈출문과 같은 전력을 써요.

- **MY**: 왼쪽 2칸 → 위 2칸 발판 `(1,1)` → `R`.
- **ME**: 왼쪽 2칸 → 아래 3칸 발판 `(1,6)` → `R`.
- **MINE**: 약 22.6초(1356틱)를 그냥 서서 기다린 다음 왼쪽 2칸 → 아래 7칸
  발판 `(1,10)` → `R`. 이 지독하게 긴 대기가 곧 "문이 열리는 시각"입니다.
- **I**: 아래 3칸 → 오른쪽 10칸 → 위 4칸 버튼0 `(13,2)` `E` → 아래 8칸 →
  오른쪽 4칸 버튼1 `(17,10)` `E` → 위 7칸 → 오른쪽 1칸 버튼2 `(18,3)` `E` →
  아래 3칸 → **오른쪽 6칸으로 레이저 줄을 건너** `(24,6)`에서 대기 →
  문이 열리면 코어 `(26,3)` → 문칸 후퇴 → 탈출 `(26,10)`.

이 방의 함정은 여기입니다. 문이 열리는 순간 레이저와 눈이 되살아나요.
그러니 나는 문이 열리기 **전에** 이미 레이저 동쪽에 가 있어야 합니다.
세 번째 잔상의 22.6초 대기가 나에게 그 시간을 벌어 줘요. 앞선 세 막이 여기서 한꺼번에 요구됩니다.

---

## 8. 스테이지별 기록

모두 par 이내, 경보 0회, 초기화 0회로 통과한 실측치입니다.
틱은 그 스테이지에서 실제로 굴린 모든 루프의 합이에요.

| # | 이름 | PAR | 쓴 잔상 | 총 틱 | 경보 | 메달 |
| --- | --- | --- | --- | --- | --- | --- |
| 01 | THE CELL | 1 | 1 | 343 | 0 | ★ |
| 02 | THE CLOCK | 1 | 1 | 713 | 0 | ★ |
| 03 | THE WEIGHT | 1 | 1 | 586 | 0 | ★ |
| 04 | THE ANTECHAMBER | 2 | 2 | 1217 | 0 | ★ |
| 05 | THE CORRIDOR | 2 | 2 | 2014 | 0 | ★ |
| 06 | THE GRATE | 2 | 2 | 2244 | 0 | ★ |
| 07 | THE SEAL | 2 | 2 | 1017 | 0 | ★ |
| 08 | THE VIGIL | 2 | 2 | 1155 | 0 | ★ |
| 09 | THE ORDER | 2 | 2 | 1734 | 0 | ★ |
| 10 | THE BEAM | 2 | 2 | 1069 | 0 | ★ |
| 11 | THE TIMETABLE | 3 | 3 | 3114 | 0 | ★ |
| 12 | THE THRESHOLD | 3 | 3 | 2988 | 0 | ★ |
| 13 | THE BREAKER | 3 | 3 | 1669 | 0 | ★ |
| 14 | THE JUNCTION | 3 | 3 | 2457 | 0 | ★ |
| 15 | THE LAST DOOR | 3 | 3 | 3447 | 0 | ★ |

---

## 9. 이 기록을 어떻게 만들었는가

사람 손으로 친 게 아닙니다. 개발 모드에만 붙어 있는 자동화 훅(`window.__imm`)으로
틱 단위 입력 큐를 먹여서 돌렸어요. 시뮬레이션 자체는 손으로 치는 것과 완전히 같은 코드이고,
규칙을 건너뛰거나 상태를 조작한 곳은 한 군데도 없습니다.
다만 0.6초짜리 창을 매번 정확히 맞히는 건 사람에게는 어려운 일이라는 점은 분명히 해 둡니다.

경로는 저장소의 검증된 정답 테이프에서 가져와 먼저 헤드리스로 15개 전부 클리어를 확인한 뒤,
실제 브라우저에서 같은 입력으로 처음부터 끝까지 다시 돌렸습니다.
8스테이지의 성공 구간(262~314틱)과 레버 몸의 허용 지연(0~164틱)은
출발 시각을 2틱씩 바꿔 가며 전수 조사해서 잰 값이에요.
