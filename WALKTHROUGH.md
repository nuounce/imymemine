# I.MY.ME.MINE — 전 스테이지 공략 & QA 보고서

> 이 문서의 모든 숫자는 **헤드리스 시뮬레이션에서 실측한 값**이다. 추정치는 없다.
> 재현: `npx tsx --test tests/qa.test.ts` (표·경고가 그대로 출력된다)
> 근거 테스트: `tests/qa.test.ts` · 정답 테이프: `tests/solutions.ts`
> (= `tests/solvability.test.ts` 의 사본. 두 파일이 어긋나면 qa 테스트가 먼저 깨진다)

단위: **1틱 = 1/60초**. 한 루프의 최대 길이는 3600틱(60초)이다.
걷기는 **한 타일 = 정확히 16틱**이라, 아래 공략의 "오른쪽 11타일"은 176틱을 뜻한다.

---

## 1. 요약 표 (실측)

| # | ID | 이름 | par | 사용 잔상 | 클리어 틱 | 여유 틱 | 여유 % | 알람 | 손 관용 폭 | 경비 |
|---|----|------|-----|-----------|-----------|---------|--------|------|------------|------|
| 1 | `01_I` | THE CELL | 1 | 1 | 275 (4.6초) | 3325 | 92.4% | 0 | −5 ~ +24↑ | — |
| 2 | `02_I` | THE CLOCK | 1 | 1 | 428 (7.1초) | 3172 | 88.1% | 0 | −4 ~ +24↑ | — |
| 3 | `03_I` | THE WEIGHT | 1 | 1 | 437 (7.3초) | 3163 | 87.9% | 0 | ±24↑ | — |
| 4 | `04_I` | THE ANTECHAMBER | 2 | 2 | 775 (12.9초) | 2825 | 78.5% | 0 | ±24↑ | — |
| 5 | `05_MY` | THE CORRIDOR | 2 | 2 | 795 (13.3초) | 2805 | 77.9% | 0 | −11 ~ +24↑ | SENTRY |
| 6 | `06_MY` | THE GRATE | 2 | 2 | 1239 (20.7초) | 2361 | 65.6% | 0 | ±24↑ | HOUND |
| 7 | `07_MY` | THE SEAL | 2 | 2 | 759 (12.7초) | 2841 | 78.9% | 0 | ±24↑ | BRUTE |
| 8 | `08_MY` | THE VIGIL | 2 | 2 | 849 (14.2초) | 2751 | 76.4% | 0 | **−8 ~ +1** | BRUTE + SENTRY |
| 9 | `09_ME` | THE ORDER | 2 | 2 | 1065 (17.8초) | 2535 | 70.4% | 0 | **−2 ~ +3** ◀ | SENTRY + WATCHER |
| 10 | `10_ME` | THE BEAM | 2 | 2 | 587 (9.8초) | 3013 | 83.7% | 0 | −11 ~ +24↑ | BRUTE + WATCHER |
| 11 | `11_ME` | THE TIMETABLE | 3 | 3 | 1289 (21.5초) | 2311 | 64.2% | 0 | −13 ~ +24↑ | HOUND + WATCHER |
| 12 | `12_ME` | THE THRESHOLD | 3 | 3 | 795 (13.3초) | 2805 | 77.9% | 0 | ±24↑ | BRUTE + HOUND |
| 13 | `13_MINE` | THE BREAKER | 3 | 3 | 781 (13.0초) | 2819 | 78.3% | 0 | **−1** ~ +24↑ | BRUTE + HOUND + WATCHER |
| 14 | `14_MINE` | THE JUNCTION | 3 | 3 | 1115 (18.6초) | 2485 | 69.0% | 0 | −19 ~ **+7** | HOUND + BRUTE + WATCHER |
| 15 | `15_MINE_FINAL` | THE LAST DOOR | 3 | 3 | 1791 (29.9초) | 1809 | 50.2% | 0 | **−3** ~ +24↑ | HOUND + BRUTE + WATCHER |

**표 읽는 법**

- **여유 틱** = 3600 − 클리어 틱. 마지막(클리어하는) 루프가 60초 제한 안에서 남긴 시간.
- **손 관용 폭** = 잔상은 그대로 두고 **마지막 테이프만** 몇 틱 늦거나 이르게 쳐도 여전히
  클리어되는가. `−2 ~ +3` 은 "2틱 일찍 ~ 3틱 늦게"까지만 통한다는 뜻이다.
  탐색 범위가 ±24틱(±0.4초)이라 `+24↑` 는 "적어도 24틱 늦어도 된다"를 의미한다.
- **음수 쪽이 실제 난이도다.** 늦는 것은 대체로 안전하다 — 잔상은 테이프가 끝나면 그
  자리에 정지해 발판을 계속 누르기 때문이다. 반대로 **이른 것은 거의 항상 죽는다**
  (문이 아직 안 열렸거나 경비가 아직 안 비켰다).

---

## 2. 발견 사항

### (a) 타이밍이 지나치게 빡빡한 스테이지 — 60초 기준으로는 **없음**

여유가 5% 미만인 스테이지는 하나도 없다. 가장 빡빡한 `15_MINE_FINAL` 도 50.2%(1809틱)가
남는다. 시간 제한 자체는 어느 스테이지에서도 압박이 아니다.

**다만 그 지표만으로는 "손으로 못 친다"를 잡지 못한다.** 남은 시간이 30초여도 통과 창이
3틱이면 사람은 못 맞춘다. 그래서 같은 질문을 정밀도로 다시 재서(위 "손 관용 폭") 얻은 결과:

| 스테이지 | 관용 폭 | 판정 |
|---|---|---|
| `09_ME` THE ORDER | **6틱 (100ms)** | ◀ **경고** — 유일하게 프레임 단위 정밀도를 요구한다 |
| `08_MY` THE VIGIL | 10틱 (−8 ~ +1) | 늦는 쪽 여유가 1틱뿐. 격자방 진입이 한 박자만 늦어도 덩치에게 걸린다 |
| `14_MINE` THE JUNCTION | 27틱 (−19 ~ +7) | 늦는 쪽 7틱. 문칸에서 물러나는 박자가 좁다 |
| `13_MINE` / `15_MINE_FINAL` | 이른 쪽 1틱 / 3틱 | 폭 자체는 넓지만 **먼저 움직이면 즉사**한다 |

`09_ME` 는 SENTRY 순찰선(y5)을 두 번 흘려보내며 벽장 셋을 순서대로 밟는 구조라, 두 번의
대기(240틱·80틱)가 순찰 위상에 정확히 맞아야 한다. 손으로 칠 수 있는 창이 100ms 다.
**이번 QA에서 사람 손 기준으로 가장 위험한 스테이지는 이 하나다.**

### (b) 한 번도 안 나오는 경비 유형 — **없음**

| 유형 | 스테이지 수 | 등장 스테이지 |
|---|---|---|
| SENTRY | 3 | `05_MY`, `08_MY`, `09_ME` |
| HOUND | 6 | `06_MY`, `11_ME`, `12_ME`, `13_MINE`, `14_MINE`, `15_MINE_FINAL` |
| BRUTE | 7 | `07_MY`, `08_MY`, `10_ME`, `12_ME`, `13_MINE`, `14_MINE`, `15_MINE_FINAL` |
| WATCHER | 6 | `09_ME`, `10_ME`, `11_ME`, `13_MINE`, `14_MINE`, `15_MINE_FINAL` |

네 유형 모두 실제로 배치돼 있다. 다만 **SENTRY 는 `09_ME` 를 끝으로 사라진다** — 3막 후반과
4막 전체에 기본형이 한 번도 나오지 않는다. 고장은 아니지만, "기준값"을 후반에 다시 보여
주지 않으면 나머지 셋의 특이함이 무엇에 대한 특이함인지 흐려진다. 밸런스 판단 사항으로 남긴다.

### (c) 눈뽕 아이템(`flashes`) 배치 — **0개 스테이지. 만들어 놓고 쓸 데가 없다.**

`levels.ts` 전체에서 `flashes` 라는 글자가 한 번도 나오지 않는다(grep 0건).
시뮬·픽업·`IN_FLASH` 재생·잔상이 자기가 주운 것을 그대로 터뜨리는 동작까지 전부 구현돼 있고
`tests/flash.test.ts` 가 그것을 검증하지만, **플레이 중에는 아이템이 화면에 등장하지 않는다.**

이 아이템의 설계 의도("과거의 내가 정확한 순간에 터뜨려 주고 지금의 내가 그 틈으로 지나간다",
`constants.ts` §눈뽕)가 가장 잘 살 자리는 위 (a)에서 걸린 `09_ME` 다 — WATCHER 를 150틱
멈추면 100ms 창이 넓어진다. 배치 판단은 레벨 담당의 몫이므로 여기서는 사실만 보고한다.

### (d) 도피로 없는 BRUTE — **없음** (7개 배치 전부 성립)

BRUTE 는 40px 라 32px 통로에 물리적으로 못 들어간다. 각 순찰선에서 **BRUTE 시야 거리(6타일)**
안에 "40px 는 못 들어가는데 몸(24px)은 들어가는 칸"이 실제로 존재하는지 타일맵으로 확인했다.

| 스테이지 | 순찰선 | 도피 칸 수 | 최근접 (거리) |
|---|---|---|---|
| `07_MY` | (15,2)→(21,2) | 17 | (22,4) 2타일 |
| `08_MY` | (16,2)→(16,10) | 2 | (20,6) 4타일 |
| `10_ME` | (12,2)→(12,8) | 1 | (9,5) 3타일 |
| `12_ME` | (14,2)→(21,2) | 18 | (11,5) 3타일 |
| `13_MINE` | (24,2)→(26,2) | 3 | (20,5) 4타일 |
| `14_MINE` | (31,2)→(31,8) | 2 | (26,5) 5타일 |
| `15_MINE_FINAL` | (31,2)→(31,9) | **1** | (25,6) **6타일** |

`15_MINE_FINAL` 이 가장 아슬아슬하다 — 도피 칸이 금고 문칸 하나뿐이고, 그것도 시야 한계인
6타일 밖이 아니라 정확히 6타일 지점이다. 성립은 하지만 여유가 없다.
(참고: `solvability.test.ts` 는 같은 검사를 반경 12타일·웨이포인트 기준으로 느슨하게 한다.
`qa.test.ts` 는 순찰선 전체 + 6타일로 조여 다시 확인했고, 그 기준으로도 7개 전부 통과했다.)

### (e) 단독 배치된 WATCHER — **없음**

WATCHER 는 직접 잡지 않고 경보만 울리므로 혼자 서 있으면 무해하다. 6개 배치 전부 같은
스테이지에 추적형 경비가 있고, 추가로 **경보 반경 7타일 안에 그 경비의 순찰선이 실제로
들어오는지**까지 확인했다 — 6개 전부 성립한다.

### (f) 그 밖에 확인된 것

- **알람 0회.** 15 스테이지 전부 경보를 한 번도 울리지 않고 클리어된다.
- **par 정확히 소진.** 15개 전부 사용 잔상 = par 다. par 미만으로 깨지는 경로는 확인되지 않았다.
- **설계 전제 유지.** 정답의 마지막 테이프를 **잔상 0개로** 돌리면 15개 전부 클리어되지 않는다.
  (`rules.test.ts` 의 단독 공략 시도와 별개로, 반대편에서 같은 것을 확인했다.)
- **문서 오류 1건 — `06_MY`.** `solvability.test.ts` 의 `roles` 는 격자 지름길을 택한 잔상(ME)에
  대해 "사냥개가 물고 끝내 이 몸을 덮친다"고 적었다. 실측하면 그런 일은 일어나지 않는다:
  그 루프의 개는 `PATROL` → `INVESTIGATE` 까지만 가고 **추격(`CHASE`)에 한 번도 들어가지
  않으며**, 최근접 2.73타일까지 붙었다가 놓친다(최종 `detect=0`). 루프는 체포가 아니라
  테이프 소진으로 끝나고(825틱, `RUNNING`), 잔상은 시체가 아니라 **산 채로 정지**한다.
  게임 동작은 정상이고 설명만 틀렸다. (아래 6번 공략은 실측대로 적었다.)
  이 스테이지의 실제 미끼 값은 "잔상이 잡혀 준다"가 아니라 **"개를 수색으로 서쪽에 붙들어
  둔다"** 다 — 잔상이 실제로 잡히는 스테이지는 `12_ME` 하나뿐이다.

---

## 3. 스테이지별 공략

각 스테이지의 **한 줄 발상**은 `src/game/levels.ts` 의 `발상:` 주석을 그대로 인용한 것이다.
공략 순서의 루프 길이는 실측값이며, 괄호 안 좌표는 타일 좌표다.

---

### 1. THE CELL — `01_I` · par 1 · 275틱

- **장치**: 발판 1 · 게이트 1 · **경비 없음**
- **발상**: *발판을 밟은 채로 나를 남기면, 그 문은 계속 열려 있다.*
- **힌트**: 문은 안에서 열리지 않는다. 누군가는 발판 위에 남아야 한다.

**공략 순서**

1. **MY (68틱)** — 스폰(4,4)에서 왼쪽 1타일, 아래 3타일을 걸어 발판(3,7) 위에 선다.
   여기서 **바로 확정**한다. 잔상은 테이프가 끝나도 그 자리에 정지하므로 게이트(9,5)가 계속 열린다.
2. **I (275틱)** — 아래 1타일 → 오른쪽 11타일로 열린 게이트를 지나 → 위 2타일에서 코어(15,3) →
   아래 4타일로 탈출구(15,7).

**막히기 쉬운 지점**: 발판에서 한 발짝이라도 움직인 채 확정하면 문이 닫힌다. "발판 위에서
멈춘 상태로" 확정해야 한다.

---

### 2. THE CLOCK — `02_I` · par 1 · 428틱

- **장치**: 시간차 버튼 1 (90틱 유지) · 게이트 1 · **경비 없음**
- **발상**: *잔상은 **자리**가 아니라 **순간**을 맡을 수도 있다.*
- **힌트**: 버튼은 잠깐만 열어 준다. 눌리는 순간 내가 이미 문 앞에 있어야 한다.

**공략 순서**

1. **MY (285틱)** — 왼쪽 3타일, 아래 5타일로 북서 구석 버튼(1,7)까지 간 뒤 **그 자리에서 기다렸다가
   280틱에 누른다.** 도착이 아니라 *누르는 시각*을 맞추는 것이 이 루프의 일이다.
2. **I (428틱)** — 아래 2타일, 오른쪽 14타일로 게이트 바로 앞(18,4)에 **미리 가서 서 있는다.**
   281틱에 문이 열리면 오른쪽 4타일 → 위 2타일 코어(22,2) → 아래 4타일 탈출구(22,6).

**막히기 쉬운 지점**: 버튼과 게이트는 18타일 떨어져 있어 달려도 178틱 > 90틱이다.
혼자 왕복하려는 시도는 시간으로 봉쇄돼 있다 — 문 앞에서 **기다리는** 것이 정답이다.

---

### 3. THE WEIGHT — `03_I` · par 1 · 437틱

- **장치**: 발판 2 · 상자 1 · 게이트 1 · **경비 없음**
- **발상**: *발판은 무게만 묻는다. 상자 하나가 한 몸을 대신한다.*
- **힌트**: 발판은 몸의 무게를 묻지 않는다.

**공략 순서**

1. **MY (148틱)** — 왼쪽 1타일로 상자 위에 붙고 → **아래로 66틱 밀어** 상자를 (2,3)에서
   **발판 A(2,7)까지 4타일 내려보낸다**(실측: 루프 종료 시 상자 좌상단 = 타일 (2.00, 7.00)) →
   오른쪽 3타일, 아래 14틱으로 **자기는 발판 B(5,7)에 선다.**
   한 몸이 두 발판을 채우는 것이 이 루프다.
2. **I (437틱)** — 34틱 기다렸다가(상자가 자리를 잡을 때까지) 아래 3타일 → 오른쪽 15타일로
   열린 복도(8,5) 통과 → 위 3타일 코어(18,2) → 아래 5타일 탈출구(18,7).

**막히기 쉬운 지점**: 상자를 미는 방향이 어긋나면 발판을 지나쳐 버린다. 상자는 정확히 3타일
= 48틱만 밀어야 한다. 1막이라 경비가 없으니 여유를 갖고 밀어도 된다.

---

### 4. THE ANTECHAMBER — `04_I` · par 2 · 775틱

- **장치**: 발판 2 · 게이트 2 (중첩) · **경비 없음**
- **발상**: *두 번째 나는 **첫 번째 나가 연 문으로만** 들어간다 — 잔상에 순서가 생긴다.*
- **힌트**: 안쪽 문의 열쇠는 바깥 문 안에 있다.

**공략 순서**

1. **MY (116틱)** — 왼쪽 3타일, 아래 4타일로 발판(1,7)에 서서 확정. 바깥 문(9,5)이 열린다.
2. **ME (326틱)** — **130틱 기다린 뒤** 출발한다(MY 가 발판에 올라선 다음이어야 한다).
   아래 2타일 → 오른쪽 8타일로 **MY 가 연 바깥 문을 통과** → 아래 2타일로 안쪽 발판(12,7).
   안쪽 문(17,5)이 열린다.
3. **I (775틱)** — 340틱 기다린 뒤 아래 2타일 → 오른쪽 18타일로 **두 문을 연달아** 통과 →
   위 3타일 코어(22,2) → 아래 5타일 탈출구(22,7).

**막히기 쉬운 지점**: 녹화 **순서**가 곧 정답이다. ME 를 먼저 녹화하면 바깥 문이 닫혀 있어
안쪽 방에 아예 들어갈 수 없다. 순서를 틀리면 "왜 안 되지"가 아니라 "들어갈 수가 없다"가 된다.

---

### 5. THE CORRIDOR — `05_MY` · par 2 · 795틱

- **장치**: 발판 1 · 시간차 버튼 1 (90틱) · 게이트 1 · **SENTRY 1** (복도 y5, (12,5)↔(22,5))
- **발상**: *경비는 소리를 쫓는다 — 잔상은 **유인 자원**이다.*
- **힌트**: 경비는 소리를 쫓는다. 누군가 뛰어주면, 다른 누군가가 지나간다.

**공략 순서**

1. **MY (548틱)** — 위 3타일, 오른쪽 8타일, 아래 3타일로 남쪽 방에 내려간 뒤
   **달리기로 좌우 왕복 4회(각 40틱)** 해서 소음을 낸다. 경비가 복도를 버리고 남쪽으로 내려온다.
   왕복이 좌우 대칭이라 끝나면 정확히 원래 x 로 돌아오고, 그대로 오른쪽 10타일 걸어
   **발판(22,8) 위에 정지**한다. 미끼와 발판 2역.
2. **ME (643틱)** — 왼쪽 2타일, 위 7타일로 북서 구석 버튼(2,1)까지 간 뒤 **632틱에 누른다.**
3. **I (795틱)** — 340틱 기다렸다가(경비가 남쪽으로 내려간 뒤) 위 3타일, 오른쪽 15타일로
   **비워진 복도를 종주**해 게이트 앞에 선다. 632틱에 문이 열리면 위 3타일 → 왼쪽 2타일에서
   코어(17,2) → 오른쪽 5타일, 위 1타일로 금고 안 탈출구(22,1).

**막히기 쉬운 지점**: 게이트 채널이 **발판 AND 버튼**이다. 둘 중 하나만으로는 절대 안 열린다.
그리고 미끼가 소리를 내기 **전에** 복도로 들어가면 정면으로 마주친다 — 340틱은 기다리는 시간이다.

---

### 6. THE GRATE — `06_MY` · par 2 · 1239틱

- **장치**: 격자(소음 바닥) 1 · 발판 1 · 시간차 버튼 1 (240틱) · 게이트 1 · **HOUND 1** ((12,2)↔(12,8))
- **발상**: ***빠른 길은 시끄럽다.*** 격자를 지나는 지름길과 남쪽 우회로가 둘 다 성립한다.
- **힌트**: 발밑이 울리는 길과, 멀지만 조용한 길. 값은 둘 다 있다.

**공략 순서 (기본 배정 — 잔상이 시끄러운 길을 산다)**

1. **MY (180틱)** — 아래 8타일, 왼쪽 3타일로 남쪽 발판(1,10)에 서서 확정. 금고 채널의 절반.
2. **ME (825틱)** — 628틱 기다렸다가 아래 3타일 → 오른쪽 7타일(**격자 4칸을 밟고 지나가며
   10틱마다 소음**) → 아래 2타일 → **820틱에 버튼(11,7).** 개가 그 소리를 듣고 수색을 나와
   2.7타일까지 다가오지만, 실측상 **추격에는 들어가지 않고 이 몸은 잡히지 않는다** —
   그래도 개가 서쪽 수색에 묶여 있는 동안 동쪽이 비는 것이 이 루프가 사는 값이다.
   (버튼은 240틱 유지되므로 뒤에 오는 몸이 쓸 시간이 충분하다.)
3. **I (1239틱)** — 516틱 기다렸다가 **남쪽 우회로**로 아래 8타일 → 오른쪽 16타일 → 위 5타일.
   격자를 밟지 않으므로 완전 무음이다. 980틱에 게이트 앞(20,5) 도착 → 오른쪽 6타일 →
   위 3타일 코어(26,2) → 아래 8타일 탈출구(26,10).

**반대 배정도 성립한다** — 잔상이 조용한 우회로로 버튼을 누르고, **내가** 격자를 밟고 지름길로
가는 배정도 par 안에서 클리어된다(`qa.test.ts` 가 별도로 검증). 이때는 개가 남북 끝(y2·y8)에서
쉬는 창을 정확히 맞춰야 한다 — 그 지점에서 격자까지는 4.2타일이라 소음 반경(4타일) 밖이다.

**막히기 쉬운 지점**: 두 길의 차이는 약 192틱(3.2초)이다. **누가 그 값을 치를지**를 고르는 것이
이 방의 전부다. 소음을 낸 몸은 개에게 물릴 각오를 해야 하고, 대신 그 개는 다른 몸을 못 쫓는다.

---

### 7. THE SEAL — `07_MY` · par 2 · 759틱

- **장치**: 발판 1 · 시간차 버튼 1 (180틱) · 레버 1 · 게이트 1 · CCTV 1 · **BRUTE 1** ((15,2)↔(21,2))
- **발상**: *감시안을 끄는 손과 발판을 밟는 발이 **같은 몸**일 수 있다 (한 잔상 2역).*
- **힌트**: 감시안을 끄는 손과 발판을 밟는 발이 같을 수 있다.

**공략 순서**

1. **MY (117틱)** — 아래 4타일로 내려가 **레버(2,7)를 당겨 CCTV 를 끄고**, 이어서 오른쪽 3타일을
   더 걸어 **발판(6,7) 위에 정지**한다. 한 몸이 스위치와 무게 두 몫을 한다.
2. **ME (141틱)** — 위 2타일로 올라가 기다렸다가 **130틱에 버튼(4,1)**. 180틱 동안 게이트가 열린다.
3. **I (759틱)** — 100틱 기다렸다가 아래 2타일 → 오른쪽 19타일로 게이트(10,5)를 지나 금고 동쪽
   세로 통로 앞(22,5)까지 → **위 4타일로 통로 x22 를 타고 올라가 코어(22,1)를 낚아채고** →
   곧장 아래 7타일로 같은 통로를 타고 내려온다 → 왼쪽 11타일로 남쪽 탈출구(11,8).

**막히기 쉬운 지점**: 금고 위 띠(y1~3)에는 40px 짜리 BRUTE 가 갇혀 있다. **세로 통로 x22 는
1타일이라 덩치가 따라 내려오지 못한다** — 코어를 집자마자 그 통로로 떨어지는 것이 요령이다.
띠 안에서 옆으로 도망치려 하면 잡힌다.

---

### 8. THE VIGIL — `08_MY` · par 2 · 849틱 ⚠ 관용 폭 −8 ~ +1틱

- **장치**: 격자 1 (5×3) · 발판 1 · 레버 1 · 게이트 1 · CCTV 1 · **BRUTE 1** ((16,2)↔(16,10)) + **SENTRY 1** ((23,2)↔(23,10))
- **발상**: ***스위치가 조용히 갈 수 없는 곳에 있다.*** 감시안을 끄러 가는 발소리가 곧 경비를 부른다.
- **힌트**: 저 스위치까지 가는 길은 조용할 수가 없다.

**공략 순서**

1. **MY (52틱)** — 왼쪽 2타일, 위 1타일로 북서 발판(1,1)에 서서 확정. 금고문(20,6)이 계속 열린다.
2. **ME (245틱)** — 아래 4타일, 오른쪽 11타일로 **격자판 한복판의 레버(14,6)** 까지 간다.
   격자 위를 걷는 것만으로 소음이 나므로 덩치가 이쪽으로 온다 — 그게 값이다. 감시안이 꺼진다.
3. **I (849틱)** — **270틱 기다린다**(덩치가 남쪽 끝으로 내려가는 창). 아래 4타일 → 오른쪽 21타일로
   격자방을 가로질러 **(20,6) 한 칸으로 빠져나가** 금고 안으로 → 위 4타일 코어(24,2) →
   아래 8타일 탈출구(24,10). 금고 안 SENTRY 를 세로로 앞질러야 한다.

**막히기 쉬운 지점**: 이 방은 **늦는 쪽 여유가 1틱뿐이다**(실측 −8 ~ +1). 270틱 대기가 한 박자만
길어져도 덩치가 돌아와 격자방 한가운데에서 마주친다. 반대로 조금 이른 것은 8틱까지 버틴다 —
망설이지 말고 살짝 이르게 들어가는 편이 안전하다.

---

### 9. THE ORDER — `09_ME` · par 2 · 1065틱 ⚠⚠ 관용 폭 6틱(100ms) — 가장 어렵다

- **장치**: 순차 버튼 3 · 발판 2 · 게이트 3 · **SENTRY 1** (문줄 y5, (13,5)↔(20,5)) + **WATCHER 1** ((21,9), 금고문 앞)
- **발상**: ***순서를 밟으려면 문을 잡아 줄 사람이 먼저 있어야 한다.***
- **힌트**: 순서는 되돌릴 수 없다. 문을 잡아 줄 사람부터 남겨라.

**공략 순서**

1. **MY (84틱)** — 아래 5타일로 서쪽 발판(2,11)에 서서 확정. **벽장 B 의 문(12,4)** 을 붙잡는다.
2. **ME (388틱)** — 아래 5타일, 오른쪽 19타일로 동쪽 발판(21,11)에 서서 확정.
   **벽장 C 의 문(19,4)** 을 붙잡는다. 두 발판은 지도 양 끝이라 한 몸으로는 겹칠 수 없다.
3. **I (1065틱)** — 오른쪽 3타일, 위 3타일로 **버튼 0(5,3)** (벽장 A 는 원래 열려 있다) →
   아래 2타일로 내려와 **240틱을 죽인다** (문줄 y5 의 순찰이 동쪽으로 지나갈 때까지) →
   오른쪽 7타일, 위 2타일로 **버튼 1(12,3)** → 아래 2타일, **80틱 대기** →
   오른쪽 7타일, 위 2타일로 **버튼 2(19,3)** → 순서 완성으로 금고문(23,9)이 열린다 →
   아래 6타일, 오른쪽 7타일로 금고 진입 → 위 2타일 코어(26,7) → 아래 4타일 탈출구(26,11).

**막히기 쉬운 지점**: **틀린 순서를 누르면 그룹이 0 으로 되돌아간다.** 되감기가 없다.
게다가 두 번의 대기(240틱·80틱)가 순찰 위상에 정확히 맞아야 해서, 실측 관용 폭이 **6틱(100ms)**
— 15 스테이지 중 유일하게 프레임 단위 정밀도를 요구한다. 금고문 앞 WATCHER 는 직접 잡지
않지만, 보이면 문줄의 SENTRY 를 불러온다. 문 앞에서 머뭇거리는 시간이 그대로 위험이다.

---

### 10. THE BEAM — `10_ME` · par 2 · 587틱

- **장치**: 레이저 1 (주기 140틱 / 점등 50틱) · 발판 1 · 시간차 버튼 1 (200틱) · 게이트 1 · **BRUTE 1** ((12,2)↔(12,8)) + **WATCHER 1** ((17,5))
- **발상**: *레이저는 완전히 예측 가능하다 — 외우는 게 아니라 **통과할 시각을 고른다.***
- **힌트**: 빛은 규칙적이다. 규칙 사이로 나를 밀어 넣으면 된다.

**공략 순서**

1. **MY (84틱)** — 왼쪽 1타일, 아래 4타일로 서쪽 발판(2,8). 레이저를 건널 필요가 없는 자리다.
2. **ME (397틱)** — **20틱 늦게 출발**해 자기 위상으로 복도를 건넌다. 아래 1타일 → 오른쪽 14타일 →
   위 3타일 → 기다렸다가 **392틱에 버튼(17,2)**. 게이트가 200틱 열린다.
3. **I (587틱)** — **40틱 늦게 출발** — 세 몸이 각자 다른 통과 위상을 갖는다.
   아래 1타일 → 오른쪽 16타일 → 문이 열리면 오른쪽 4타일 → 위 3타일 코어(23,2) →
   아래 6타일 탈출구(23,8).

**막히기 쉬운 지점**: 레이저는 140틱 중 50틱만 켜지고 **난수가 없다.** 복도를 건너는 데 필요한
겹침은 12틱 남짓이므로, 외울 것은 패턴이 아니라 **출발 시각**이다. 20틱·40틱이라는 값 자체가
정답의 일부다. 금고문 앞 WATCHER 때문에 문 앞에서 기다리는 시간은 짧을수록 좋다.

---

### 11. THE TIMETABLE — `11_ME` · par 3 · 1289틱

- **장치**: 순차 버튼 3 · 시간차 버튼 2 (각 90틱) · 발판 1 · 게이트 3 · **HOUND 1** ((19,6)↔(19,9)) + **WATCHER 1** ((25,10))
- **발상**: ***잔상들이 시각표를 나눠 갖는다.*** 각자 다른 시각에 다른 문을 90틱씩 연다.
- **힌트**: 문마다 주인이 다르다. 나는 그 사이를 밟고 지나갈 뿐이다.

**공략 순서**

1. **MY (455틱)** — 아래 5타일, 오른쪽 22타일로 남동쪽 버튼(24,11)까지 간 뒤
   **450틱에 눌러 벽장 A 의 문(6,4)을 90틱 연다.**
2. **ME (700틱)** — 위 1타일로 서쪽 버튼(2,5) 옆에 서서 **695틱에 눌러 벽장 B 의 문(15,4)을 연다.**
3. **MINE (100틱)** — 아래 5타일, 왼쪽 1타일로 발판(1,11). 탈출 게이트 채널의 나머지 절반이다.
4. **I (1289틱)** — 250틱 기다렸다가 오른쪽 8타일로 **버튼 0(10,6)** →
   왼쪽 4타일, 위 3타일로 **열려 있는 A 에 들어가 버튼 1(6,3)** →
   아래 3타일, 오른쪽 9타일, 위 3타일로 **열려 있는 B 에서 버튼 2(15,3)** →
   아래 7타일로 내려와 **150틱 대기**(금고문 앞 목이 비기를 기다린다) →
   오른쪽 14타일로 금고문(26,10) 통과 → 위 2타일 코어(29,8) → 아래 3타일 탈출구(29,11).

**막히기 쉬운 지점**: 버튼과 그 문은 각각 18타일·13타일 떨어져 있어 **달려도 90틱 안에 닿지
못한다.** 문은 반드시 다른 몸이 열어 줘야 한다. 벽장이 한 칸만 깊은 것은 90틱 안에
들어갔다 나올 수 있게 하기 위해서다(진입 16 + 상승 16 + 누름 2 + 하강 16 + 이탈 16 = 66틱).
150틱 대기를 건너뛰고 금고문 앞에 서면 WATCHER 에게 보이고, 그러면 사냥개가 붙는다 —
개는 한번 물면 300틱을 쫓는다.

---

### 12. THE THRESHOLD — `12_ME` · par 3 · 795틱

- **장치**: 발판 2 · 상자 1 · 시간차 버튼 1 (150틱) · 레버 1 · 게이트 1 · CCTV 1 · **BRUTE 1** (북쪽 (14,2)↔(21,2)) + **HOUND 1** (남쪽 (16,8)↔(23,8))
- **발상**: *상자·레버·버튼·미끼가 **전부 다른 몸**에 걸린다.*
- **힌트**: 넷이 전부다. 더 남길 나는 없다.

**공략 순서**

1. **MY (290틱)** — 오른쪽 4타일, 위 1타일로 돌아 상자 오른쪽에 붙고 **왼쪽으로 90틱 밀어
   서쪽 벽에 박는다**(상자가 발판(1,2)에 얹힌다) → 아래 6타일, 왼쪽 20틱으로 내려와
   **자기는 발판(1,8)에 선다.** 한 몸이 발판 둘을 채운다.
2. **ME (711틱)** — 아래 2타일, 오른쪽 9타일로 **레버(12,5)를 당겨 CCTV 차단** → 오른쪽 4타일 →
   기다렸다가 **700틱에 탈출 게이트 버튼(16,5).** 걷기만 한다 — 소음을 내면 경비가 복도로 온다.
3. **MINE (1192틱, 체포됨)** — 180틱 기다렸다가(CCTV 가 꺼진 뒤 — 알람이 울리면 미끼가 무의미해진다)
   남쪽 방으로 내려가 **달리며 좌우로 진동**한다. 사냥개가 물고, 결국 **이 몸은 잡힌다.**
   잡힌 잔상은 재생 동안 살아 있는 미끼로 기능하고 재생이 끝나면 그 자리에 시체로 남는다.
   개를 서쪽 끝에 묶어 두는 것이 이 몸의 값이다.
4. **I (795틱)** — 200틱 기다렸다가 아래 2타일 → **오른쪽 23타일로 복도를 종주** →
   아래 3타일, 왼쪽 2타일로 남쪽 코어(24,8) → 오른쪽 2타일 → 위 6타일로 세로 통로를 타고
   북쪽 방 탈출구(25,2).

**막히기 쉬운 지점**: 탈출 게이트 채널은 **발판 2개 AND 버튼**이라 상자 하나로는 절대 못 채운다.
그리고 두 방을 잇는 길은 1타일짜리 동쪽 세로 통로와 1타일 높이 복도 y5 뿐이라 **덩치는 두 방
사이를 오갈 수 없다** — 북쪽 방이 그의 영역이고 탈출구가 거기 있다. 통로를 탈 때 덩치가 어디
있는지가 전부다.

---

### 13. THE BREAKER — `13_MINE` · par 3 · 781틱 ⚠ 이른 쪽 여유 1틱

- **장치**: 발판 3 · 레버 1 · 게이트 1 · CCTV 1 · **전력 버스 1** (`eye` ↔ `door`) · **BRUTE 1** ((24,2)↔(26,2)) + **HOUND 1** ((24,8)↔(26,8)) + **WATCHER 1** ((26,5), 금고 안쪽)
- **발상**: ***문을 열면 감시안이 켜진다.*** 전력은 하나뿐이라 둘을 동시에 가질 수 없다.
- **힌트**: 전력은 하나뿐이다. 문과 눈 중 하나만 고를 수 있다.

**공략 순서**

1. **MY (277틱)** — 아래 5타일, 오른쪽 5타일로 **레버(8,8)를 당겨 감시안을 끄고**,
   왼쪽 7타일로 되돌아와 **남쪽 발판(1,8)에서 대기**한다. 2역.
2. **ME (68틱)** — 왼쪽 2타일, 위 2타일로 북서 발판(1,1).
3. **MINE (524틱)** — 456틱 기다렸다가 왼쪽 2타일, 아래 2타일로 **520틱에 마지막 발판(1,5)을 밟는다.**
   이 순간 발판 셋이 동시에 차서 **전력이 눈에서 문으로 넘어간다** — 금고문이 열리고 눈이 다시 뜬다.
4. **I (781틱)** — 200틱 기다렸다가(레버가 내려간 뒤) 아래 2타일 → 오른쪽 16타일로
   **눈이 감긴 동안 복도를 건너** 게이트 앞(19,5)에 선다 → 520틱에 문이 열리면 오른쪽 5타일 →
   가운데 줄(24,5)에서 **50틱을 죽인다**(덩치가 동쪽 끝으로 물러날 때까지) →
   위 3타일로 북쪽 띠의 코어(24,2)를 찌르고 → 아래 6타일로 곧장 남쪽 띠의 탈출구(24,8).

**막히기 쉬운 지점**: 이 방의 핵심은 **눈이 다시 뜨는 그 순간 내가 이미 시야 밖에 있어야 한다**는
것이다. 그래서 복도를 건너는 일이 문이 열리기 **전에** 끝나 있어야 한다.
실측 관용 폭은 이른 쪽으로 **1틱**뿐이다 — 200틱보다 먼저 출발하면 레버가 아직 안 내려가
CCTV 에 걸린다. 늦는 쪽은 넉넉하다. 금고 안은 북쪽 띠에 덩치, 남쪽 띠에 사냥개가 있고
가운데 줄만 비어 있으므로, **두 띠를 다른 시각에 한 번씩** 찌르는 수밖에 없다.

---

### 14. THE JUNCTION — `14_MINE` · par 3 · 1115틱 ⚠ 늦는 쪽 여유 7틱

- **장치**: 발판 3 · 레버 2 · 레이저 1 (주기 180 / 점등 150) · 게이트 1 · CCTV 1 · **전력 버스 1** (`las` ↔ `eye` ↔ `door`) · **HOUND 1** (분기실 (19,3)↔(19,7)) + **BRUTE 1** (금고 안쪽 (31,2)↔(31,8)) + **WATCHER 1** ((32,5))
- **발상**: *위험이 셋인데 전력은 하나다 — **셋을 시간으로 나눠 통과한다.***
- **힌트**: 레이저, 눈, 문. 셋 다 끌 수는 없으니 하나씩 지나간다.

**공략 순서**

1. **MY (197틱)** — 위 2타일, 오른쪽 4타일로 **레버(7,1)를 켜 레이저를 끈다** →
   왼쪽 6타일로 되돌아와 발판(1,1). 2역.
2. **ME (481틱)** — 아래 5타일, 오른쪽 4타일로 가서 기다렸다가 **380틱에 레버(7,8)로 감시안을 끈다.**
   같은 버스이므로 **그 순간 레이저가 되살아난다** → 왼쪽 6타일로 발판(1,8). 2역.
3. **MINE (664틱)** — 596틱 기다렸다가 왼쪽 2타일, 아래 2타일로 **660틱에 마지막 발판(1,5).**
   문이 전력을 가져가고 눈이 다시 뜬다.
4. **I (1115틱)** — 150틱 기다렸다가 아래 2타일 → 오른쪽 22타일.
   이 한 줄 안에 **레이저가 죽은 동안 x13 을 건너고**, **눈이 감긴 동안** 분기실(x17~21)의
   사냥개를 흘려보내며 CCTV 구간을 지나는 일이 전부 들어 있다 → 660틱에 문이 열리면
   오른쪽 1타일로 **문칸(26,5)** → **240틱 대기**(금고 안쪽 덩치가 물러나기를 기다린다) →
   오른쪽 1타일, 위 3타일로 코어(27,2)를 찌르고 → 아래 3타일, 왼쪽 1타일로 **문칸으로 후퇴** →
   20틱 → 오른쪽 1타일, 아래 3타일로 탈출구(27,8).

**막히기 쉬운 지점**: 레버를 켜는 **순서**가 곧 내가 복도를 통과하는 순서다 —
나중에 켜진 쪽이 전력을 가져가므로, 레이저 → 눈 → 문의 순서로 위험이 하나씩 열렸다 닫힌다.
분기실은 출입구가 (16,5)·(22,5) 두 칸뿐이라 **한번 물리면 빠져나갈 데가 없다.**
금고에서는 **문칸(26,5)이 유일한 피난처**다(40px 가 못 들어온다). 코어와 탈출구를 한 번에
둘 다 집으려 하지 말고, 한 번씩 찌르고 매번 그 칸으로 물러나야 한다.
실측 관용 폭은 늦는 쪽으로 **7틱**뿐이다.

---

### 15. THE LAST DOOR — `15_MINE_FINAL` · par 3 · 1791틱 ⚠ 이른 쪽 여유 3틱

- **장치**: 발판 3 · 순차 버튼 3 · 레이저 1 (주기 160 / 점등 100) · 게이트 1 · CCTV 1 · **전력 버스 1** (`seq` ↔ `exit`) · **HOUND 1** (순서방 (22,10)↔(22,2)) + **BRUTE 1** (금고 안쪽 (31,2)↔(31,9)) + **WATCHER 1** ((32,6))
- **발상**: ***코드를 끝내면 길이 열리고, 문을 열면 그 길이 다시 닫힌다.***
- **힌트**: 문이 열리는 순간, 지나온 길은 닫힌다. 먼저 건너가 있어라.

**공략 순서**

1. **MY (1504틱)** — 1500틱까지 기다렸다가 왼쪽 2타일, 위 2타일로 발판(1,1).
2. **ME (1504틱)** — 같은 시각에 발판(1,6).
3. **MINE (1504틱)** — 같은 시각에 발판(1,10). **셋이 동시에 눌리는 1500틱에
   전력이 `seq` 에서 `exit` 으로 넘어간다** — 문이 열리고 레이저와 감시안이 되살아난다.
4. **I (1791틱)** — 아래 3타일, 오른쪽 10타일, 위 4타일로 **버튼 0(13,2)** → 100틱 대기 →
   아래 8타일, 오른쪽 4타일로 **버튼 1(17,10)** → 120틱 대기 →
   위 7타일, 오른쪽 1타일로 **버튼 2(18,3)** → 순서 완성으로 `seq` 가 켜져
   **레이저(x20)와 감시안이 함께 죽는다** → 40틱 → 아래 3타일, 오른쪽 6타일로
   **죽은 레이저를 건너 게이트 앞(24,6)에 선다** → 1500틱에 문이 열리면(그리고 레이저가
   되살아나면) 오른쪽 1타일로 **문칸(25,6)** → 60틱 대기 → 오른쪽 1타일, 위 3타일로
   코어(26,3) → 아래 3타일, 왼쪽 1타일로 **문칸 후퇴** → 20틱 → 오른쪽 1타일,
   아래 4타일로 탈출구(26,10).

**막히기 쉬운 지점**: 이 방의 함정은 **문이 열리는 순간 내가 아직 서쪽에 있으면 끝**이라는 것이다.
`seq` 와 `exit` 이 같은 버스라, 문이 전력을 가져가면 레이저(x20)가 되살아나 돌아갈 길도
갈 길도 막힌다. **문이 열리기 전에 반드시 레이저 동쪽에 서 있어야 한다.**
세 번의 대기(100·120·40틱)는 x22 를 세로로 오가는 사냥개가 비켜 주기를 기다리는 시간이고,
마지막 두 번(60·20틱)은 금고 문칸에서 덩치가 물러나기를 기다리는 시간이다.
금고 안 도피 칸은 **문칸(25,6) 하나뿐**이며 덩치 순찰선에서 정확히 6타일 —
시야 한계선이다. 여유가 가장 적은 배치다.

---

## 4. 부록 — 경비 4종 읽는 법

| 유형 | 크기 | 시야 | 특징 | 대처 |
|---|---|---|---|---|
| **SENTRY** | 26px | 7타일 / 50° | 기준값. 순찰 대기 중 좌우로 두리번거린다 | 정면과 스윕 폭을 함께 피한다 |
| **HOUND** | 24px | 5.5타일 / 35° | 시야는 좁지만 **냄새(최근 궤적 320틱)를 되짚고** 한번 물면 300틱을 쫓는다 | 도망치지 말고 **다른 궤적으로 따돌린다** — 잔상이 지나간 길도 냄새다 |
| **BRUTE** | 40px | 6타일 / 65° | 느리지만 시야가 넓고 감지가 빠르다. **32px 통로에 물리적으로 못 들어간다** | 1타일 통로로 들어가 버린다. AI 예외가 아니라 충돌 판정의 결과다 |
| **WATCHER** | 28px | 10타일 / 40° | **움직이지도 잡지도 않는다.** 게이지가 차면 경보를 울려 반경 7타일의 경비를 부른다 | 문 앞에서 **머뭇거리지 않는 것**이 유일한 대처다 |

공통: 달리면 감지 이득이 2배가 되고, 시야를 벗어나도 게이지는 천천히(틱당 1) 식는다 —
**한 번 들킨 사실은 잠깐 숨는 것으로 지워지지 않는다.**

## 5. 부록 — 장치 요약

| 장치 | 규칙 |
|---|---|
| **발판(plate)** | 무게만 묻는다. 몸·**시체**·**상자** 어느 것이든 누른다. 채널은 기본 **AND** — 발판이 둘이면 둘 다 눌려야 열린다 |
| **시간차 버튼(button)** | 누른 뒤 `holdTicks` 동안만 ON. 90 / 150 / 180 / 200 / 240틱 짜리가 쓰인다 |
| **레버(lever)** | 토글. 10틱 잠금이 있어 한 틱에 연타되지 않는다 |
| **격자(grate)** | 그 위를 **움직이면** 걷든 뛰든 소음(10틱마다, 반경 4타일). 서 있으면 무음 |
| **레이저(laser)** | `(틱 + 위상) % 주기 < 점등` — 난수가 없다. 외울 것은 패턴이 아니라 통과 시각 |
| **순차 버튼(seqButton)** | 그룹을 order 오름차순으로 밟아야 한다. **틀리면 0으로 되돌아간다** |
| **전력 버스(powerBus)** | 묶인 채널 중 **동시에 하나만** ON. 나중에 켜진 쪽이 전력을 가져간다 |
| **눈뽕(flash)** | 1회용, 반경 5타일, 150틱 실명. 테이프에 녹화되므로 잔상도 자기가 주웠던 것을 그대로 터뜨린다. **— 현재 어느 스테이지에도 배치돼 있지 않다 (§2-c)** |
