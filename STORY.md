# I.MY.ME.MINE — 서사 설정집 v1

> 이 문서가 스토리의 단일 출처다. 대사·단서·엔딩을 쓸 때 여기 정의된 사실만 쓴다.
> 게임 규칙은 `SPEC.md`, 스테이지 실측은 `WALKTHROUGH.md`.

---

## 1. 한 문장

**억제 코어는 능력을 누르는 장치가 아니라 모으는 장치다.
당신의 탈출 시도 자체가 생산 공정이고, 당신이 남긴 잔상이 제품이다.**

---

## 2. 이 전제가 기존 메커니즘을 다시 읽게 한다

새 규칙을 더하지 않는다. **이미 있는 것의 의미만 뒤집는다.** 이게 이 방향을 고른 이유다.

| 지금까지 플레이어가 이해한 것 | 수확 전제에서 사실인 것 |
|---|---|
| 게임 오버가 없다 = 관대한 설계 | **그들은 당신을 죽일 생각이 없다.** 당신은 재고다 |
| 잡히면 루프를 하나 잃는다 | 불량이 나서 **라인을 되감은** 것이다 |
| `WATCHER` 는 안 쫓아오고 부르기만 한다 | 경비가 아니라 **품질 관리**다. 수율을 보고 있다 |
| 잔상이 그 자리에 남는다 | 회수되기 전까지 **적재된 상태**다 |
| 시체도 발판을 누른다 | 불량품도 **자재로는 쓸모가 있다** |
| 60초 제한 | **한 사이클**이다 |
| `R` 로 나를 남긴다 | 생산 버튼이다 |
| `DEBT` 는 점수다 | **당신이 버린 것 = 그들이 가져간 것** |

마지막 줄이 엔딩의 축이다. 전체 초기화(`Backspace`)로 실수를 지울 때마다,
플레이어는 자기도 모르게 **제품을 넘겨주고 있었다.**

---

## 3. 용어

| 게임 안 표기 | 실제 의미 | 노출 시점 |
|---|---|---|
| `CORE` (억제 코어) | 수확 장치. 누르는 게 아니라 모은다 | 3막 |
| `WARDEN` / `SENTRY` 등 | 경비가 아니라 **공정 인원** | 2막 암시 |
| `WATCHER` | 품질 관리 | 2막 막간 |
| 배치(batch) | 한 번의 수용 주기. 벽의 금 한 무더기 | 3막 막간 |
| 회수 | 한 배치에서 회수된 잔상 수 (내부 문서·소설은 구 표기 `수율` 유지 — 화면 노출은 전부 `회수` 계열, STORY-TEXT-V2 §7) | 1막 막간부터 |

**주의**: 시설은 잔인하게 굴지 않는다. **사무적**이다. 그게 더 무섭다.
문서 어투는 감정이 없고 짧다. 고문·학대 묘사를 넣지 마라 — 이 이야기의 공포는 폭력이 아니라 **회계**다.

---

## 4. 막간 만화 (막 사이 2컷)

인트로(`intro.ts`)의 7컷 시스템을 재사용한다. 각 막간은 **2컷**, 약 5초, 아무 키나 스킵.
대사는 인트로와 같은 구어체 혼잣말. 설명하지 말고 **주인공이 알아채게** 하라.

### 막간 1 — 1막 → 2막 · "문 바깥쪽"
- **컷 1** 방금 빠져나온 문을 돌아본다. 바깥쪽에는 **손잡이가 달려 있다.**
- **컷 2** 문 옆 벽에 붙은 종이. 손으로 적은 표. 숫자 몇 개와 `회수`.

> `...문 바깥쪽엔 손잡이가 있네.`
> *나오라고 열어 둔 거였어?*

### 막간 2 — 2막 → 3막 · "선반"
- **컷 1** 넓고 낮은 방. 선반이 줄지어 있고 칸마다 멈춘 실루엣이 하나씩.
- **컷 2** 그중 하나가 **내가 조금 전에 취한 자세 그대로** 멈춰 있다.

> `저거, 아까 내가 한 자세잖아.`
> *여기 다 모아 놨네. 내가 버리고 온 것들.*

### 막간 3 — 3막 → 4막 · "셈"
- **컷 1** 벽의 금 클로즈업. 넷씩 묶인 무더기가 화면 밖까지 이어진다.
- **컷 2** 무더기 아래에 작게 적힌 일련번호. 인쇄체다. 손글씨가 아니다.

> `넷씩 묶은 게... 이게 몇 무더기야.`
> *내가 센 게 아니었구나. 세어진 거였고.*

### 막간 4 — 엔딩 직전 · "마지막 문"
- **컷 1** 바깥문 앞. 문 너머는 아직 안 보인다.
- **컷 2** 뒤를 돌아본다. 지나온 복도에 내가 남긴 것들이 서 있다.

> `여기서 나가면 끝이야.`
> *...전부 데리고 나갈 수는 없나.*

---

## 5. 환경 단서

`E` 로 읽는다. **강제하지 않는다** — 건너뛰는 사람은 몰라도 되고, 찾는 사람은 더 알게 된다.
읽어도 시뮬레이션 상태는 바뀌지 않는다(테이프 무영향).

어투는 **사무적**이다. 감정을 넣지 마라.

- **1막** `점검표 — 잠금 해제 확인 / 조명 정상 / 대상 각성 확인`
- **2막** `배치 기록 — 회수 3. 손실 1. 손실분 자재 전용.`
- **2막** (낙서, 손글씨) `여기까지 온 건 나만이 아니다`
- **3막** `코어 상태 — 축적률 정상. 대상 협조적.`
- **3막** `주의 — 대상이 코어를 인지할 경우 회수량이 급증함. 인지를 막지 말 것.`
- **4막** `공정 요약 — 배치당 평균 회수 2.7. 목표 3.0.`
- **4막** (낙서) `문은 열려 있었다. 나는 그게 함정인 줄 알았다.`

3막의 `인지를 막지 말 것` 이 핵심 단서다 — **당신이 진실을 알수록 더 많이 생산한다.**
알아챈 순간 더 절박해지고, 절박해지면 잔상을 더 쓴다.

---

## 6. 엔딩 — `DEBT` 분기

`GAUNTLET` 완주 시 누적 `DEBT` 로 갈린다. `STORY` 모드는 마지막 스테이지 클리어 시 같은 기준을 쓴다.

### `DEBT 0` — 아무것도 두고 오지 않았다
넷이 함께 문을 나선다. 시설의 회수량 **0**.
> `아무것도 안 두고 왔어. 하나도.`

마지막 화면에 그 배치의 회수량 `0` 이 찍히고, 처음으로 **손글씨가 아닌 인쇄체가 흔들린다.**

### `DEBT 1~2` — 나왔지만 두고 왔다
당신은 나간다. 문이 닫히고, 안쪽에서 회수반이 들어가는 소리.
> `...몇은 두고 왔네.`

### `DEBT 3 이상` — 수확은 성공했다
당신은 나갔다. 문이 닫힌다. 그리고 안에서 **다음 배치가 깨어나는 소리**가 들린다.
마지막에 인쇄체 한 줄이 찍힌다:
> `회수 양호.`

**이 결말이 가장 아프도록 쓴다.** 플레이어가 실수를 지우려고 `Backspace` 를 누를 때마다
제품을 넘겨주고 있었다는 게 여기서 드러난다. 벽의 금이 하나 더 그어지며 인트로로 되돌아간다.

---

## 7. 속말의 변화

막이 오를수록 잔상을 부르는 말이 바뀐다. **설명하지 말고 호칭만 바꾼다.**

| 막 | 호칭 | 예 |
|---|---|---|
| 1막 | 사물 | `저거`, `저 자리` |
| 2막 | 3인칭 | `쟤`, `저쪽의 나` |
| 3막 | 망설임 | `쟤도... 나였지` |
| 4막 | 1인칭 복수 | `우리` |

4막에서 처음으로 `우리` 가 나온다. 그 한 단어가 이 이야기의 도착점이다.

### 예외 — 막간 2 의 `것들` 은 의도다 (고치지 말 것)

막간 2(`interlude.ts`)의 `여기 다 모아 놨네. 내가 버리고 온 것들.` 은
잔상을 **`것들`(사물)로 부른다.** 위 사다리로 보면 2막에서 1막으로 한 단계 후퇴다.

**후퇴가 연출이다.** 선반에 재고처럼 진열된 걸 본 순간 주인공이 잠깐 **시설의 시선에
물든다** — 그 컷의 공포는 진열이 아니라 "내가 지금 그들처럼 보고 있다"에서 나온다.
3막의 `쟤도... 나였지` 가 그것을 되돌리며, 되돌아오기 때문에 4막의 `우리` 가 도착이 된다.

일관성 오류로 오해해 `쟤들`·`애들` 로 고치지 마라. 고치면 사다리는 매끄러워지지만
그 컷은 아무것도 하지 않게 된다.

### 목표 표지판도 같은 사다리를 탄다

HUD 상단 목표 줄(`currentObjective`)은 **주인공이 아는 만큼만** 말한다.
코어의 정체 노출 시점이 3막이므로(§3 용어표), 1~2막에는 이름을 부르지 않는다.

| 막 | 목표 줄 |
|---|---|
| 1~2막 | `▸ 저 물건부터` |
| 3막~ | `▸ 억제 코어부터` |
| 코어 획득 후 | `▸ 탈출구로` |

막 판단은 레벨 id 의 슬롯 토큰(`01_I` → `I`)에서 나온다. 스테이지 번호를 세지 않는다.
명령형(`손에 넣어라`)을 쓰지 않는 이유는 §8 에 있다.

---

## 8. 쓰지 말 것

- 폭력·고문·유혈 묘사. 이 이야기의 공포는 **회계**다.
- 악당의 연설. 시설은 얼굴이 없다. 문서와 숫자로만 존재한다.
- 주인공이 진실을 말로 정리하는 대사("그러니까 이곳은 나를 이용해서…"). **보여주고 끝낸다.**
- 인트로에서 이미 쓴 문장의 재사용. 속말 중복 금지 테스트가 있다.
- **대사 속의 키 이름**(`R`, `F`, `Q`, `SHIFT` …). 주인공은 키캡을 볼 수 없다 —
  그건 플레이어의 손에 있는 물건이지 이 세계의 물건이 아니다. 조작을 가르치는 일은
  화면이 맡는다: 상황별 키 프롬프트(`hud.ts` 의 `PROMPT_COPY`)가 필요한 순간에
  그 키 하나를 키캡으로 띄우고, `조작법` 패널이 전부를 모아 둔다.
  **대사에서 키를 뺄 때는 반드시 그 키가 프롬프트나 조작법에 있는지 확인한다** —
  말에도 없고 화면에도 없으면 플레이어는 그 조작을 영영 모른다.
- **명령조**(`~해라`, `~하시오`). 시설은 얼굴이 없고 주인공은 혼잣말만 한다.
  HUD 목표 줄까지 포함해 화면의 한국어는 **그의 목소리 하나**다 — 목표는 짧은
  명사구로 쓴다(`▸ 탈출구로`). 예외는 시설이 **문서로** 말하는 것뿐이다:
  환경 단서(§5)·`회수 양호.` 같은 인쇄체 한 줄, 그리고 영문 스텐실 계기 라벨.
